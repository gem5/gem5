---
name: Docker images build and push

#on:
#  push:
#    branches:
#      - 'develop'
#    paths:
#      - util/dockerfiles/**
on:
    workflow_dispatch:


jobs:

    obtain-targets:
        runs-on: ubuntu-latest
        outputs:
            targets: ${{ steps.generate.outputs.targets }}

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: List targets
              id: generate
              uses: docker/bake-action/subaction/list-targets@v4
              with:
                  # Normally 'default'. This is for testing.
                  target: gcc-version-14
                  workdir: util/dockerfiles

    docker-buildx-bake:
        runs-on: [self-hosted, linux, x64]
        needs:
            - obtain-targets

        strategy:
            fail-fast: false
            matrix:
                target: ${{ fromJSON(needs.obtain-targets.outputs.targets) }}

        permissions:
            packages: write
            contents: read

        steps:

            - name: Set up QEMU
              uses: docker/setup-qemu-action@v3

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v3

            - name: Login to GitHub Container Registry
              uses: docker/login-action@v3
              with:
                  registry: ghcr.io
                  username: ${{ github.repository_owner }}
                  password: ${{ secrets.GITHUB_TOKEN }}

            - name: Checkout Bake File
              uses: actions/checkout@v4
              with:
                  sparse-checkout: |
                      util/dockerfiles/docker-bake.hcl
                  sparse-checkout-cone-mode: false

            - name: Pull Image Cache
              working-directory: util/dockerfiles
              run: |
                  docker pull $(docker buildx bake ${{ matrix.target }} --print \
                  | jq -rc ".[\"target\"][\"${{ matrix.target }}\"][\"cache-from\"][0][\"ref\"]") || true


            - name: Bake and Push
              uses: docker/bake-action@v6
              with:
                  source: '{{defaultContext}}:util/dockerfiles'
                  targets: ${{ matrix.target }}
                  push: true
