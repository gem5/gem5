# CHI Protocol with different ISAs test

The purpose of this test is to ensure the CHI protocol functions across ARM, X86, and RISCV ISA targets.
In addition it tests CHI functions correctly in both timing and atomic and for differing numbers of CPU cores.
