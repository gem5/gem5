# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv/insts:amo"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv/insts:bs"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv/insts:compressed"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv/insts:mem"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv/insts:standard"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv/insts:static_inst"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv/insts:vector"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv/insts:zcmp"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv/insts:zcmt"],
        "//conditions:default": [],
    }) + [
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "amo",
    srcs = ["amo.cc"],
    hdrs = ["amo.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/riscv/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/riscv:utility",
        "//src/cpu:exec_context",
        "//src/cpu:static_inst",
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv/insts:mem"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv/insts:static_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "bs",
    srcs = ["bs.cc"],
    hdrs = ["bs.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/riscv/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/arch/riscv:utility"] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv/insts:static_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "compressed",
    srcs = ["compressed.cc"],
    hdrs = ["compressed.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/riscv/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/riscv:utility",
        "//src/cpu:static_inst",
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv/insts:static_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "mem",
    srcs = ["mem.cc"],
    hdrs = ["mem.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/riscv/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/riscv:utility",
        "//src/cpu:exec_context",
        "//src/cpu:static_inst",
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv/insts:static_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "standard",
    srcs = ["standard.cc"],
    hdrs = ["standard.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/riscv/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/riscv:utility",
        "//src/arch/riscv/regs:misc",
        "//src/cpu:exec_context",
        "//src/cpu:static_inst",
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv/insts:static_inst"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv:faults"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv:semihosting"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "static_inst",
    srcs = ["static_inst.cc"],
    hdrs = ["static_inst.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/riscv/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/riscv:pcstate",
        "//src/arch/riscv:types",
        "//src/arch/riscv:workaround_that_cuts_circular_dependency__isa",
        "//src/arch/riscv/regs:misc",
        "//src/cpu:exec_context",
        "//src/cpu:static_inst",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "vector",
    srcs = ["vector.cc"],
    hdrs = ["vector.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/riscv/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/riscv:utility",
        "//src/arch/riscv:workaround_that_cuts_circular_dependency__isa",
        "//src/arch/riscv/regs:misc",
        "//src/arch/riscv/regs:vector",
        "//src/cpu:exec_context",
        "//src/cpu:static_inst",
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv/insts:static_inst"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv:faults"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "zcmp",
    srcs = ["zcmp.cc"],
    hdrs = ["zcmp.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/riscv/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/riscv:utility",
        "//src/arch/riscv/regs:int",
        "//src/cpu:static_inst",
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv/insts:static_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "zcmt",
    srcs = ["zcmt.cc"],
    hdrs = ["zcmt.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/riscv/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/riscv:pcstate",
        "//src/arch/riscv:utility",
        "//src/arch/riscv/regs:int",
        "//src/cpu:static_inst",
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv/insts:static_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
