# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "fs_workload",
    srcs = ["fs_workload.cc"],
    hdrs = ["fs_workload.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/freebsd",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/generic/freebsd:threadinfo",
        "//src/base/loader:dtb_file",
        "//src/base/loader:symtab",
        "//src/base/loader:workaround_that_cuts_circular_dependency__object_file",
        "//src/cpu:workaround_that_cuts_circular_dependency__base",
        "//src/cpu:workaround_that_cuts_circular_dependency__pc_event",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/generated/debug:Loader",
        "//src/generated/params:ArmFsFreebsd",
        "//src/kern/freebsd:events",
        "//src/mem:physical",
        "//src/sim:stat_control",
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm:fs_workload"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm:utility"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "se_workload",
    srcs = ["se_workload.cc"],
    hdrs = ["se_workload.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/freebsd",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/arm:page_size",
        "//src/arch/arm:se_workload",
        "//src/arch/arm/freebsd",
        "//src/arch/arm/regs:cc",
        "//src/base:trace",
        "//src/base/loader:workaround_that_cuts_circular_dependency__object_file",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/generated/params:ArmEmuFreebsd",
        "//src/mem:workaround_that_cuts_circular_dependency__se_translating_port_proxy",
        "//src/sim:syscall_desc",
        "//src/sim:syscall_emul",
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/regs:int"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm:process"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "freebsd",
    srcs = [],
    hdrs = ["freebsd.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/freebsd",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/kern/freebsd",
        "//src/kern/linux:flag_tables",
        "//src/sim:byteswap",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
