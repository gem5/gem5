# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "int",
    srcs = ["int.cc"],
    hdrs = ["int.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/regs",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/arm:types",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/cpu:reg_class",
        "//src/generated/debug:IntRegs",
        "//src/sim:core",
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/regs:misc"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm:isa"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm:utility"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "misc",
    srcs = ["misc.cc"],
    hdrs = ["misc.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/regs",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/arm:types",
        "//src/arch/arm/regs:misc_types",
        "//src/base:bitfield",
        "//src/base:compiler",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/cpu:reg_class",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/dev/arm:generic_timer_miscregs_types",
        "//src/generated/debug:MiscRegs",
        "//src/generated/params:ArmISA",
        "//src/sim:full_system",
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/insts:misc64"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm:isa"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/dev/arm:gic_v3_cpu_interface"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "cc",
    srcs = [],
    hdrs = ["cc.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/regs",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/cpu:reg_class",
        "//src/generated/debug:CCRegs",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "mat",
    srcs = [],
    hdrs = ["mat.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/regs",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/arm:matrix",
        "//src/arch/arm:types",
        "//src/cpu:reg_class",
        "//src/generated/debug:MatRegs",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "misc_accessors",
    srcs = [],
    hdrs = ["misc_accessors.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/regs",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/arm/regs:misc_types",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "misc_types",
    srcs = [],
    hdrs = ["misc_types.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/regs",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/base:bitunion"],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "vec",
    srcs = [],
    hdrs = ["vec.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/regs",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/arm:types",
        "//src/arch/generic:vec_pred_reg",
        "//src/arch/generic:vec_reg",
        "//src/cpu:reg_class",
        "//src/generated/debug:VecPredRegs",
        "//src/generated/debug:VecRegs",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
