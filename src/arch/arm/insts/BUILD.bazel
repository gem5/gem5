# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "fplib",
    srcs = ["fplib.cc"],
    hdrs = ["fplib.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/base:workaround_that_cuts_circular_dependency__logging"] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/regs:misc"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "branch",
    srcs = ["branch.cc"],
    hdrs = ["branch.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/base:cprintf"] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/insts:pred_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "branch64",
    srcs = ["branch64.cc"],
    hdrs = ["branch64.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/insts:static_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "crypto",
    srcs = ["crypto.cc"],
    hdrs = ["crypto.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "data64",
    srcs = ["data64.cc"],
    hdrs = ["data64.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/base:trace"] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/insts:static_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "macromem",
    srcs = ["macromem.cc"],
    hdrs = ["macromem.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/arm:pcstate",
        "//src/arch/arm/generated:decoder",
        "//src/arch/arm/insts:neon64_mem",
        "//src/base:compiler",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/insts:pred_inst"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm:tlb"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "mem",
    srcs = ["mem.cc"],
    hdrs = ["mem.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/arm:pcstate",
        "//src/base/loader:symtab",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/insts:pred_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "mem64",
    srcs = ["mem64.cc"],
    hdrs = ["mem64.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/arm:pcstate",
        "//src/base/loader:symtab",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/mem:request",
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/insts:misc64"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/insts:static_inst"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm:tlb"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "misc",
    srcs = ["misc.cc"],
    hdrs = ["misc.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/cpu:reg_class"] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/insts:pred_inst"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm:tlbi_op"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "misc64",
    srcs = ["misc64.cc"],
    hdrs = ["misc64.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/arm:types",
        "//src/generated/debug:MiscRegs",
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/insts:static_inst"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm:isa"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm:mmu"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm:tlbi_op"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "pred_inst",
    srcs = ["pred_inst.cc"],
    hdrs = ["pred_inst.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/arm:pcstate",
        "//src/base:compiler",
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/insts:static_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "pseudo",
    srcs = ["pseudo.cc"],
    hdrs = ["pseudo.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/cpu:exec_context"] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/insts:static_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "sme",
    srcs = ["sme.cc"],
    hdrs = ["sme.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/insts:static_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "static_inst",
    srcs = ["static_inst.cc"],
    hdrs = ["static_inst.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/arm:pcstate",
        "//src/base:condcodes",
        "//src/base:cprintf",
        "//src/base:trace",
        "//src/base/loader:symtab",
        "//src/cpu:exec_context",
        "//src/cpu:reg_class",
        "//src/cpu:static_inst",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/sim:byteswap",
        "//src/sim:full_system",
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm:faults"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm:isa"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm:self_debug"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm:system"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm:utility"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "sve",
    srcs = ["sve.cc"],
    hdrs = ["sve.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/insts:static_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "sve_mem",
    srcs = ["sve_mem.cc"],
    hdrs = ["sve_mem.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/insts:static_inst"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm:tlb"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "tme64",
    srcs = ["tme64.cc"],
    hdrs = ["tme64.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/generated/debug:ArmTme"] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/insts:macromem"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/insts:pred_inst"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/insts:static_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "tme64ruby",
    srcs = ["tme64ruby.cc"],
    hdrs = [],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/generic:memhelpers",
        "//src/generated/debug:ArmTme",
        "//src/mem:packet_access",
        "//src/mem:request",
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/insts:tme64"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm:faults"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm:htm"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "vfp",
    srcs = ["vfp.cc"],
    hdrs = ["vfp.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/arm:pcstate",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/insts:misc"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/regs:misc"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "neon64_mem",
    srcs = [],
    hdrs = ["neon64_mem.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
