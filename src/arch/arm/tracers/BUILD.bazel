# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/tracers:capstone"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/tracers:tarmac_base"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/tracers:tarmac_parser"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/tracers:tarmac_record"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/tracers:tarmac_record_v8"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/tracers:tarmac_tracer"],
        "//conditions:default": [],
    }) + [
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
] + select({
    "//src/generated/flags:use_capstone_is_true": [
        macro_sim_object(
            "ArmCapstone.py",
            enums = [],
            sim_objects = ["ArmCapstoneDisassembler"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_arm_isa_is_true": [
        macro_sim_object(
            "TarmacTrace.py",
            enums = ["TarmacDump"],
            sim_objects = [
                "TarmacParser",
                "TarmacTracer",
            ],
        ),
    ],
    "//conditions:default": [],
}) + [
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "capstone",
    srcs = ["capstone.cc"],
    hdrs = ["capstone.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/tracers",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:output",
        "//src/generated/params:ArmCapstoneDisassembler",
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/insts:static_inst"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_capstone_is_true": ["//src/cpu:capstone"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "tarmac_base",
    srcs = ["tarmac_base.cc"],
    hdrs = ["tarmac_base.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/tracers",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/arm:types",
        "//src/base:trace",
        "//src/base:types",
        "//src/cpu:reg_class",
        "//src/cpu:static_inst",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/sim:insttracer",
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/regs:misc"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "tarmac_parser",
    srcs = ["tarmac_parser.cc"],
    hdrs = ["tarmac_parser.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/tracers",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/arm/regs:vec",
        "//src/base:trace",
        "//src/base:types",
        "//src/cpu:static_inst",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/generated/params:TarmacParser",
        "//src/mem:request",
        "//src/mem:translating_port_proxy",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/mem:workaround_that_cuts_circular_dependency__port_proxy",
        "//src/mem:workaround_that_cuts_circular_dependency__se_translating_port_proxy",
        "//src/sim:core",
        "//src/sim:cur_tick",
        "//src/sim:faults",
        "//src/sim:full_system",
        "//src/sim:insttracer",
        "//src/sim:sim_exit",
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/insts:static_inst"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/regs:int"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm:mmu"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "tarmac_record",
    srcs = ["tarmac_record.cc"],
    hdrs = ["tarmac_record.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/tracers",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:printable",
        "//src/cpu:reg_class",
        "//src/cpu:static_inst",
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/insts:static_inst"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/regs:misc"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/tracers:tarmac_base"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "tarmac_record_v8",
    srcs = ["tarmac_record_v8.cc"],
    hdrs = ["tarmac_record_v8.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/tracers",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/insts:static_inst"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/tracers:tarmac_tracer"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm:mmu"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "tarmac_tracer",
    srcs = ["tarmac_tracer.cc"],
    hdrs = ["tarmac_tracer.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/arm/tracers",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:output",
        "//src/cpu:workaround_that_cuts_circular_dependency__base",
        "//src/generated/enums:TarmacDump",
        "//src/generated/params:TarmacTracer",
        "//src/sim:insttracer",
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/tracers:tarmac_record"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm/tracers:tarmac_record_v8"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/arch/arm:system"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
