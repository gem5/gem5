# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/arch/amdgpu/vega:faults"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/arch/amdgpu/vega:pagetable"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/arch/amdgpu/vega:pagetable_walker"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/arch/amdgpu/vega:tlb"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/arch/amdgpu/vega:tlb_coalescer"],
        "//conditions:default": [],
    }) + [
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
] + select({
    "//src/generated/flags:build_gpu_is_true": [
        macro_sim_object(
            "VegaGPUTLB.py",
            enums = [],
            sim_objects = [
                "VegaPagetableWalker",
                "VegaGPUTLB",
                "VegaTLBCoalescer",
            ],
        ),
    ],
    "//conditions:default": [],
}) + [
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "faults",
    srcs = ["faults.cc"],
    hdrs = ["faults.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/amdgpu/vega",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/generic:workaround_that_cuts_circular_dependency__mmu",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/sim:faults",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "pagetable",
    srcs = ["pagetable.cc"],
    hdrs = ["pagetable.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/amdgpu/vega",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/amdgpu/vega:page_size",
        "//src/base:bitunion",
        "//src/base:types",
        "//src/sim:serialize",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "pagetable_walker",
    srcs = ["pagetable_walker.cc"],
    hdrs = ["pagetable_walker.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/amdgpu/vega",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:types",
        "//src/generated/debug:GPUPTWalker",
        "//src/generated/params:VegaPagetableWalker",
        "//src/mem:packet_access",
        "//src/mem:workaround_that_cuts_circular_dependency__abstract_mem",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/sim:workaround_that_cuts_circular_dependency__clocked_object",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/arch/amdgpu/vega:faults"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/arch/amdgpu/vega:pagetable"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/arch/amdgpu/vega:tlb"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "tlb",
    srcs = ["tlb.cc"],
    hdrs = ["tlb.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/amdgpu/vega",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/amdgpu/common:gpu_translation_state",
        "//src/arch/generic:workaround_that_cuts_circular_dependency__mmu",
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__statistics",
        "//src/generated/debug:GPUPrefetch",
        "//src/generated/debug:GPUTLB",
        "//src/generated/params:VegaGPUTLB",
        "//src/mem:port",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/sim:workaround_that_cuts_circular_dependency__clocked_object",
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/arch/amdgpu/vega:faults"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/arch/amdgpu/vega:pagetable"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/arch/amdgpu/vega:pagetable_walker"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_x86_isa_is_true": ["//src/dev/amdgpu:amdgpu_device"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "tlb_coalescer",
    srcs = ["tlb_coalescer.cc"],
    hdrs = ["tlb_coalescer.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/amdgpu/vega",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/amdgpu/common:gpu_translation_state",
        "//src/arch/generic:workaround_that_cuts_circular_dependency__mmu",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/base:workaround_that_cuts_circular_dependency__statistics",
        "//src/generated/debug:GPUTLB",
        "//src/generated/params:VegaTLBCoalescer",
        "//src/mem:port",
        "//src/mem:request",
        "//src/sim:workaround_that_cuts_circular_dependency__clocked_object",
        "//src/sim:workaround_that_cuts_circular_dependency__process",
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/arch/amdgpu/vega:pagetable"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/arch/amdgpu/vega:tlb"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "page_size",
    srcs = [],
    hdrs = ["page_size.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/amdgpu/vega",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/base:types"],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
