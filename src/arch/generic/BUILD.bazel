# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
        "//src/arch/generic:decoder",
        "//src/arch/generic:htm",
        "//src/arch/generic:mmu",
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/generic:semihosting"],
        "//conditions:default": [],
    }) + [
        "//src/arch/generic/freebsd:lib",
        "//src/arch/generic/linux:lib",
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    macro_sim_object(
        "BaseISA.py",
        enums = [],
        sim_objects = ["BaseISA"],
    ),
    macro_sim_object(
        "BaseInterrupts.py",
        enums = [],
        sim_objects = ["BaseInterrupts"],
    ),
    macro_sim_object(
        "BaseMMU.py",
        enums = [],
        sim_objects = ["BaseMMU"],
    ),
    macro_sim_object(
        "InstDecoder.py",
        enums = [],
        sim_objects = ["InstDecoder"],
    ),
    macro_sim_object(
        "BaseTLB.py",
        enums = ["TypeTLB"],
        sim_objects = ["BaseTLB"],
    ),
] + select({
    "//src/generated/flags:use_riscv_isa_is_true": [
        macro_sim_object(
            "BaseSemihosting.py",
            enums = [],
            sim_objects = ["BaseSemihosting"],
        ),
    ],
    "//conditions:default": [],
}) + [
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        "//src/arch/generic/freebsd:sim_objects",
        "//src/arch/generic/linux:sim_objects",
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "decoder",
    srcs = ["decoder.cc"],
    hdrs = ["decoder.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/generic",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/generic:pcstate",
        "//src/base:bitfield",
        "//src/base:intmath",
        "//src/base:types",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/cpu:static_inst_fwd",
        "//src/generated/params:InstDecoder",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "htm",
    srcs = ["htm.cc"],
    hdrs = ["htm.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/generic",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/mem:htm"],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "mmu",
    srcs = ["mmu.cc"],
    hdrs = ["mmu.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/generic",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/generic:tlb",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/generated/params:BaseMMU",
        "//src/mem:request",
        "//src/mem:translation_gen",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "semihosting",
    srcs = ["semihosting.cc"],
    hdrs = ["semihosting.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/generic",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:output",
        "//src/base:time",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/dev/serial",
        "//src/generated/debug:Semihosting",
        "//src/generated/params:BaseSemihosting",
        "//src/mem:physical",
        "//src/mem:workaround_that_cuts_circular_dependency__port_proxy",
        "//src/sim:byteswap",
        "//src/sim:core",
        "//src/sim:guest_abi",
        "//src/sim:pseudo_inst",
        "//src/sim:sim_exit",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "debugfaults",
    srcs = [],
    hdrs = ["debugfaults.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/generic",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/cpu:null_static_inst",
        "//src/cpu:static_inst",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/sim:faults",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "decode_cache",
    srcs = [],
    hdrs = ["decode_cache.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/generic",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:types",
        "//src/cpu:decode_cache",
        "//src/cpu:static_inst_fwd",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "interrupts",
    srcs = [],
    hdrs = ["interrupts.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/generic",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/generated/params:BaseInterrupts",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "isa",
    srcs = [],
    hdrs = ["isa.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/generic",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/generic:pcstate",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/cpu:reg_class",
        "//src/mem:request",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "memhelpers",
    srcs = [],
    hdrs = ["memhelpers.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/generic",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:types",
        "//src/mem:request",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/sim:byteswap",
        "//src/sim:insttracer",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "workaround_that_cuts_circular_dependency__mmu",
    srcs = [],
    hdrs = ["mmu.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/generic",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/generated/params:BaseMMU",
        "//src/mem:request",
        "//src/mem:translation_gen",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "pcstate",
    srcs = [],
    hdrs = ["pcstate.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/generic",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:trace",
        "//src/base:types",
        "//src/sim:serialize",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "tlb",
    srcs = [],
    hdrs = ["tlb.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/generic",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/generic:workaround_that_cuts_circular_dependency__mmu",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/generated/enums:TypeTLB",
        "//src/generated/params:BaseTLB",
        "//src/mem:request",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "vec_pred_reg",
    srcs = [],
    hdrs = ["vec_pred_reg.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/generic",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:cprintf",
        "//src/base:types",
        "//src/sim:serialize_handlers",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "vec_reg",
    srcs = [],
    hdrs = ["vec_reg.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/generic",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:cprintf",
        "//src/base:types",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/sim:serialize_handlers",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
