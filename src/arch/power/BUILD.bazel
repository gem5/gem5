# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
    ] + select({
        "//src/generated/flags:use_power_isa_is_true": ["//src/arch/power:decoder"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_power_isa_is_true": ["//src/arch/power:faults"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_power_isa_is_true": ["//src/arch/power/insts:branch"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_power_isa_is_true": ["//src/arch/power/insts:condition"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_power_isa_is_true": ["//src/arch/power/insts:floating"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_power_isa_is_true": ["//src/arch/power/insts:integer"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_power_isa_is_true": ["//src/arch/power/insts:mem"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_power_isa_is_true": ["//src/arch/power/insts:static_inst"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_power_isa_is_true": ["//src/arch/power:isa"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_power_isa_is_true": ["//src/arch/power/linux:se_workload"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_power_isa_is_true": ["//src/arch/power:pagetable"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_power_isa_is_true": ["//src/arch/power:process"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_power_isa_is_true": ["//src/arch/power:remote_gdb"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_power_isa_is_true": ["//src/arch/power:se_workload"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_power_isa_is_true": ["//src/arch/power:tlb"],
        "//conditions:default": [],
    }) + [
        "//src/arch/power/gdb-xml:lib",
        "//src/arch/power/insts:lib",
        "//src/arch/power/linux:lib",
        "//src/arch/power/regs:lib",
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
] + select({
    "//src/generated/flags:use_power_isa_is_true": [
        macro_sim_object(
            "PowerCPU.py",
            enums = [],
            sim_objects = [],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_power_isa_is_true": [
        macro_sim_object(
            "PowerDecoder.py",
            enums = [],
            sim_objects = ["PowerDecoder"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_power_isa_is_true": [
        macro_sim_object(
            "PowerISA.py",
            enums = [],
            sim_objects = ["PowerISA"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_power_isa_is_true": [
        macro_sim_object(
            "PowerInterrupts.py",
            enums = [],
            sim_objects = ["PowerInterrupts"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_power_isa_is_true": [
        macro_sim_object(
            "PowerMMU.py",
            enums = [],
            sim_objects = ["PowerMMU"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_power_isa_is_true": [
        macro_sim_object(
            "PowerSeWorkload.py",
            enums = [],
            sim_objects = [
                "PowerSEWorkload",
                "PowerEmuLinux",
            ],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_power_isa_is_true": [
        macro_sim_object(
            "PowerTLB.py",
            enums = [],
            sim_objects = ["PowerTLB"],
        ),
    ],
    "//conditions:default": [],
}) + [
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        "//src/arch/power/gdb-xml:sim_objects",
        "//src/arch/power/insts:sim_objects",
        "//src/arch/power/linux:sim_objects",
        "//src/arch/power/regs:sim_objects",
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "decoder",
    srcs = ["decoder.cc"],
    hdrs = ["decoder.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/power",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/generic:decode_cache",
        "//src/arch/generic:decoder",
        "//src/arch/power:types",
        "//src/cpu:static_inst",
        "//src/generated/debug:Decode",
        "//src/generated/params:PowerDecoder",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "faults",
    srcs = ["faults.cc"],
    hdrs = ["faults.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/power",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/cpu:workaround_that_cuts_circular_dependency__base",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/sim:faults",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "isa",
    srcs = ["isa.cc"],
    hdrs = ["isa.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/power",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/generic:isa",
        "//src/arch/power:pcstate",
        "//src/arch/power:types",
        "//src/arch/power/regs:float",
        "//src/arch/power/regs:int",
        "//src/arch/power/regs:misc",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/cpu:reg_class",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/generated/debug:MatRegs",
        "//src/generated/params:PowerISA",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "pagetable",
    srcs = ["pagetable.cc"],
    hdrs = ["pagetable.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/power",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:types",
        "//src/sim:serialize",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "process",
    srcs = ["process.cc"],
    hdrs = ["process.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/power",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/power:page_size",
        "//src/arch/power:types",
        "//src/arch/power/regs:int",
        "//src/arch/power/regs:misc",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/base/loader:elf_object",
        "//src/base/loader:workaround_that_cuts_circular_dependency__object_file",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/generated/debug:Stack",
        "//src/generated/params:Process",
        "//src/mem:workaround_that_cuts_circular_dependency__page_table",
        "//src/sim:aux_vector",
        "//src/sim:byteswap",
        "//src/sim:process_impl",
        "//src/sim:syscall_return",
        "//src/sim:workaround_that_cuts_circular_dependency__process",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "remote_gdb",
    srcs = ["remote_gdb.cc"],
    hdrs = ["remote_gdb.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/power",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/power:pcstate",
        "//src/arch/power/gdb-xml:gdb_xml_power64_core",
        "//src/arch/power/gdb-xml:gdb_xml_power_core",
        "//src/arch/power/gdb-xml:gdb_xml_power_fpu",
        "//src/arch/power/gdb-xml:gdb_xml_powerpc_32",
        "//src/arch/power/gdb-xml:gdb_xml_powerpc_64",
        "//src/arch/power/regs:float",
        "//src/arch/power/regs:int",
        "//src/arch/power/regs:misc",
        "//src/base:remote_gdb",
        "//src/cpu:thread_state",
        "//src/generated/debug:GDBAcc",
        "//src/generated/debug:GDBMisc",
        "//src/mem:workaround_that_cuts_circular_dependency__page_table",
        "//src/sim:byteswap",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "se_workload",
    srcs = ["se_workload.cc"],
    hdrs = ["se_workload.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/power",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/power/regs:int",
        "//src/arch/power/regs:misc",
        "//src/generated/params:PowerSEWorkload",
        "//src/sim:se_workload",
        "//src/sim:syscall_abi",
        "//src/sim:syscall_desc",
    ] + select({
        "//src/generated/flags:use_power_isa_is_true": ["//src/arch/power:remote_gdb"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "tlb",
    srcs = ["tlb.cc"],
    hdrs = ["tlb.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/power",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/generic:tlb",
        "//src/base:inifile",
        "//src/base:str",
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__statistics",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/generated/debug:Power",
        "//src/generated/debug:TLB",
        "//src/generated/params:PowerTLB",
        "//src/mem:request",
        "//src/mem:workaround_that_cuts_circular_dependency__page_table",
        "//src/sim:full_system",
        "//src/sim:workaround_that_cuts_circular_dependency__process",
    ] + select({
        "//src/generated/flags:use_power_isa_is_true": ["//src/arch/power:faults"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_power_isa_is_true": ["//src/arch/power:pagetable"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "page_size",
    srcs = [],
    hdrs = ["page_size.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/power",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/base:types"],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "pcstate",
    srcs = [],
    hdrs = ["pcstate.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/power",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/generic:pcstate",
        "//src/arch/power:types",
        "//src/generated/enums:ByteOrder",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "types",
    srcs = [],
    hdrs = ["types.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/power",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/power:pcstate",
        "//src/base:bitunion",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
