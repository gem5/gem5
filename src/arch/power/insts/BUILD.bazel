# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "branch",
    srcs = ["branch.cc"],
    hdrs = ["branch.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/power/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/power/regs:int",
        "//src/arch/power/regs:misc",
        "//src/base/loader:symtab",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
    ] + select({
        "//src/generated/flags:use_power_isa_is_true": ["//src/arch/power/insts:static_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "condition",
    srcs = ["condition.cc"],
    hdrs = ["condition.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/power/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/base:cprintf"] + select({
        "//src/generated/flags:use_power_isa_is_true": ["//src/arch/power/insts:static_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "floating",
    srcs = ["floating.cc"],
    hdrs = ["floating.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/power/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:bitfield",
        "//src/base:cprintf",
    ] + select({
        "//src/generated/flags:use_power_isa_is_true": ["//src/arch/power/insts:static_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "integer",
    srcs = ["integer.cc"],
    hdrs = ["integer.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/power/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/power/regs:misc",
        "//src/base:bitfield",
        "//src/base:cprintf",
    ] + select({
        "//src/generated/flags:use_power_isa_is_true": ["//src/arch/power/insts:static_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "mem",
    srcs = ["mem.cc"],
    hdrs = ["mem.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/power/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/base/loader:symtab"] + select({
        "//src/generated/flags:use_power_isa_is_true": ["//src/arch/power/insts:static_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "static_inst",
    srcs = ["static_inst.cc"],
    hdrs = ["static_inst.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/power/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/power:pcstate",
        "//src/arch/power:types",
        "//src/base:trace",
        "//src/cpu:reg_class",
        "//src/cpu:static_inst",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
