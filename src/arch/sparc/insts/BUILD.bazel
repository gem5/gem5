# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
    ] + select({
        "//src/generated/flags:use_sparc_isa_is_true": ["//src/arch/sparc/insts:blockmem"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_sparc_isa_is_true": ["//src/arch/sparc/insts:branch"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_sparc_isa_is_true": ["//src/arch/sparc/insts:integer"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_sparc_isa_is_true": ["//src/arch/sparc/insts:mem"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_sparc_isa_is_true": ["//src/arch/sparc/insts:micro"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_sparc_isa_is_true": ["//src/arch/sparc/insts:priv"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_sparc_isa_is_true": ["//src/arch/sparc/insts:static_inst"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_sparc_isa_is_true": ["//src/arch/sparc/insts:trap"],
        "//conditions:default": [],
    }) + [
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "blockmem",
    srcs = ["blockmem.cc"],
    hdrs = ["blockmem.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/sparc/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [] + select({
        "//src/generated/flags:use_sparc_isa_is_true": ["//src/arch/sparc/insts:micro"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "branch",
    srcs = ["branch.cc"],
    hdrs = ["branch.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/sparc/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [] + select({
        "//src/generated/flags:use_sparc_isa_is_true": ["//src/arch/sparc/insts:static_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "integer",
    srcs = ["integer.cc"],
    hdrs = ["integer.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/sparc/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [] + select({
        "//src/generated/flags:use_sparc_isa_is_true": ["//src/arch/sparc/insts:static_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "mem",
    srcs = ["mem.cc"],
    hdrs = ["mem.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/sparc/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [] + select({
        "//src/generated/flags:use_sparc_isa_is_true": ["//src/arch/sparc/insts:static_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "micro",
    srcs = ["micro.cc"],
    hdrs = ["micro.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/sparc/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [] + select({
        "//src/generated/flags:use_sparc_isa_is_true": ["//src/arch/sparc/insts:static_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "priv",
    srcs = ["priv.cc"],
    hdrs = ["priv.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/sparc/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [] + select({
        "//src/generated/flags:use_sparc_isa_is_true": ["//src/arch/sparc/insts:static_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "static_inst",
    srcs = ["static_inst.cc"],
    hdrs = ["static_inst.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/sparc/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/sparc:pcstate",
        "//src/arch/sparc:types",
        "//src/arch/sparc/regs:misc",
        "//src/base:bitunion",
        "//src/base:trace",
        "//src/cpu:exec_context",
        "//src/cpu:static_inst",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
    ] + select({
        "//src/generated/flags:use_sparc_isa_is_true": ["//src/arch/sparc/regs:int"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "trap",
    srcs = ["trap.cc"],
    hdrs = ["trap.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/sparc/insts",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [] + select({
        "//src/generated/flags:use_sparc_isa_is_true": ["//src/arch/sparc/insts:static_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
