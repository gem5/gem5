# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
    ] + select({
        "//src/generated/flags:use_mips_isa_is_true": ["//src/arch/mips:decoder"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_mips_isa_is_true": ["//src/arch/mips:dsp"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_mips_isa_is_true": ["//src/arch/mips:faults"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_mips_isa_is_true": ["//src/arch/mips:idle_event"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_mips_isa_is_true": ["//src/arch/mips:interrupts"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_mips_isa_is_true": ["//src/arch/mips:isa"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_mips_isa_is_true": ["//src/arch/mips/linux:se_workload"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_mips_isa_is_true": ["//src/arch/mips:pagetable"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_mips_isa_is_true": ["//src/arch/mips:process"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_mips_isa_is_true": ["//src/arch/mips:remote_gdb"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_mips_isa_is_true": ["//src/arch/mips:se_workload"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_mips_isa_is_true": ["//src/arch/mips:tlb"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_mips_isa_is_true": ["//src/arch/mips:utility"],
        "//conditions:default": [],
    }) + [
        "//src/arch/mips/gdb-xml:lib",
        "//src/arch/mips/linux:lib",
        "//src/arch/mips/regs:lib",
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
] + select({
    "//src/generated/flags:use_mips_isa_is_true": [
        macro_sim_object(
            "MipsCPU.py",
            enums = [],
            sim_objects = [],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_mips_isa_is_true": [
        macro_sim_object(
            "MipsDecoder.py",
            enums = [],
            sim_objects = ["MipsDecoder"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_mips_isa_is_true": [
        macro_sim_object(
            "MipsISA.py",
            enums = [],
            sim_objects = ["MipsISA"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_mips_isa_is_true": [
        macro_sim_object(
            "MipsInterrupts.py",
            enums = [],
            sim_objects = ["MipsInterrupts"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_mips_isa_is_true": [
        macro_sim_object(
            "MipsMMU.py",
            enums = [],
            sim_objects = ["MipsMMU"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_mips_isa_is_true": [
        macro_sim_object(
            "MipsSeWorkload.py",
            enums = [],
            sim_objects = [
                "MipsSEWorkload",
                "MipsEmuLinux",
            ],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_mips_isa_is_true": [
        macro_sim_object(
            "MipsTLB.py",
            enums = [],
            sim_objects = ["MipsTLB"],
        ),
    ],
    "//conditions:default": [],
}) + [
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        "//src/arch/mips/gdb-xml:sim_objects",
        "//src/arch/mips/linux:sim_objects",
        "//src/arch/mips/regs:sim_objects",
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "decoder",
    srcs = ["decoder.cc"],
    hdrs = ["decoder.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/mips",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/generic:decode_cache",
        "//src/arch/generic:decoder",
        "//src/arch/mips:types",
        "//src/base:types",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/cpu:static_inst",
        "//src/generated/debug:Decode",
        "//src/generated/params:MipsDecoder",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "dsp",
    srcs = ["dsp.cc"],
    hdrs = ["dsp.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/mips",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/mips:types",
        "//src/base:bitfield",
        "//src/base:types",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/cpu:static_inst",
        "//src/sim:serialize",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "faults",
    srcs = ["faults.cc"],
    hdrs = ["faults.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/mips",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/mips:pra_constants",
        "//src/arch/mips/regs:misc",
        "//src/base:trace",
        "//src/cpu:null_static_inst",
        "//src/cpu:workaround_that_cuts_circular_dependency__base",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/generated/debug:MipsPRA",
        "//src/mem:workaround_that_cuts_circular_dependency__page_table",
        "//src/sim:faults",
        "//src/sim:full_system",
        "//src/sim:workaround_that_cuts_circular_dependency__process",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "idle_event",
    srcs = ["idle_event.cc"],
    hdrs = ["idle_event.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/mips",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/cpu:workaround_that_cuts_circular_dependency__pc_event"],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "interrupts",
    srcs = ["interrupts.cc"],
    hdrs = ["interrupts.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/mips",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/generic:interrupts",
        "//src/arch/mips:pra_constants",
        "//src/base:compiler",
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/generated/debug:Interrupt",
        "//src/generated/params:MipsInterrupts",
        "//src/sim:serialize",
    ] + select({
        "//src/generated/flags:use_mips_isa_is_true": ["//src/arch/mips:faults"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "isa",
    srcs = ["isa.cc"],
    hdrs = ["isa.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/mips",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/generic:isa",
        "//src/arch/mips:mt",
        "//src/arch/mips:mt_constants",
        "//src/arch/mips:pcstate",
        "//src/arch/mips:pra_constants",
        "//src/arch/mips:types",
        "//src/arch/mips/regs:float",
        "//src/arch/mips/regs:int",
        "//src/arch/mips/regs:misc",
        "//src/base:bitfield",
        "//src/base:types",
        "//src/cpu:reg_class",
        "//src/cpu:workaround_that_cuts_circular_dependency__base",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/generated/debug:MatRegs",
        "//src/generated/debug:MipsPRA",
        "//src/generated/params:MipsISA",
        "//src/sim:eventq",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "pagetable",
    srcs = ["pagetable.cc"],
    hdrs = ["pagetable.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/mips",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:types",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/sim:serialize",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "process",
    srcs = ["process.cc"],
    hdrs = ["process.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/mips",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/mips:page_size",
        "//src/arch/mips/regs:int",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/base/loader:elf_object",
        "//src/base/loader:workaround_that_cuts_circular_dependency__object_file",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/generated/debug:Loader",
        "//src/generated/params:Process",
        "//src/mem:workaround_that_cuts_circular_dependency__page_table",
        "//src/sim:aux_vector",
        "//src/sim:byteswap",
        "//src/sim:process_impl",
        "//src/sim:syscall_return",
        "//src/sim:workaround_that_cuts_circular_dependency__process",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "remote_gdb",
    srcs = ["remote_gdb.cc"],
    hdrs = ["remote_gdb.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/mips",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/mips/gdb-xml:gdb_xml_mips",
        "//src/arch/mips/regs:float",
        "//src/arch/mips/regs:int",
        "//src/arch/mips/regs:misc",
        "//src/base:bitfield",
        "//src/base:remote_gdb",
        "//src/cpu:thread_state",
        "//src/generated/debug:GDBAcc",
        "//src/generated/debug:GDBMisc",
        "//src/mem:workaround_that_cuts_circular_dependency__page_table",
        "//src/sim:full_system",
    ] + select({
        "//src/generated/flags:use_mips_isa_is_true": ["//src/arch/mips:decoder"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "se_workload",
    srcs = ["se_workload.cc"],
    hdrs = ["se_workload.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/mips",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/mips/regs:int",
        "//src/generated/params:MipsSEWorkload",
        "//src/sim:se_workload",
        "//src/sim:syscall_abi",
        "//src/sim:syscall_desc",
    ] + select({
        "//src/generated/flags:use_mips_isa_is_true": ["//src/arch/mips:remote_gdb"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "tlb",
    srcs = ["tlb.cc"],
    hdrs = ["tlb.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/mips",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/generic:tlb",
        "//src/arch/mips:pra_constants",
        "//src/base:inifile",
        "//src/base:str",
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__statistics",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/generated/debug:MipsPRA",
        "//src/generated/debug:TLB",
        "//src/generated/params:MipsTLB",
        "//src/mem:request",
        "//src/mem:workaround_that_cuts_circular_dependency__page_table",
        "//src/sim:workaround_that_cuts_circular_dependency__process",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
    ] + select({
        "//src/generated/flags:use_mips_isa_is_true": ["//src/arch/mips:faults"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_mips_isa_is_true": ["//src/arch/mips:pagetable"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_mips_isa_is_true": ["//src/arch/mips:utility"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "utility",
    srcs = ["utility.cc"],
    hdrs = ["utility.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/mips",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/mips:page_size",
        "//src/arch/mips:types",
        "//src/arch/mips/regs:float",
        "//src/arch/mips/regs:int",
        "//src/arch/mips/regs:misc",
        "//src/base:bitfield",
        "//src/base:types",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/cpu:static_inst",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/sim:serialize",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "mt",
    srcs = [],
    hdrs = ["mt.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/mips",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/mips:faults",
        "//src/arch/mips:mt_constants",
        "//src/arch/mips:pcstate",
        "//src/arch/mips:pra_constants",
        "//src/arch/mips/regs:int",
        "//src/arch/mips/regs:misc",
        "//src/base:bitfield",
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/cpu:exec_context",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "mt_constants",
    srcs = [],
    hdrs = ["mt_constants.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/mips",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/mips:types",
        "//src/base:bitunion",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "page_size",
    srcs = [],
    hdrs = ["page_size.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/mips",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/base:types"],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "pcstate",
    srcs = [],
    hdrs = ["pcstate.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/mips",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/arch/generic:pcstate"],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "pra_constants",
    srcs = [],
    hdrs = ["pra_constants.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/mips",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/mips:types",
        "//src/base:bitunion",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "types",
    srcs = [],
    hdrs = ["types.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/mips",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/arch/mips:pcstate"],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
