# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
        "//src/arch/x86/bare_metal:lib",
        "//src/arch/x86/bios:lib",
        "//src/arch/x86/kvm:lib",
        "//src/arch/x86/linux:lib",
        "//src/arch/x86/regs:lib",
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        "//src/arch/x86/bare_metal:sim_objects",
        "//src/arch/x86/bios:sim_objects",
        "//src/arch/x86/kvm:sim_objects",
        "//src/arch/x86/linux:sim_objects",
        "//src/arch/x86/regs:sim_objects",
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "intmessage",
    srcs = [],
    hdrs = ["intmessage.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/x86",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/x86:x86_traits",
        "//src/base:bitunion",
        "//src/base:compiler",
        "//src/base:types",
        "//src/dev/x86:intdev",
        "//src/mem:packet_access",
        "//src/mem:request",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "page_size",
    srcs = [],
    hdrs = ["page_size.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/x86",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/base:types"],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "pcstate",
    srcs = [],
    hdrs = ["pcstate.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/x86",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/generic:pcstate",
        "//src/sim:serialize",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "se_workload",
    srcs = [],
    hdrs = ["se_workload.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/x86",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/base:types"],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "x86_traits",
    srcs = [],
    hdrs = ["x86_traits.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/x86",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/base:types"],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
