# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
    ] + select({
        "//src/generated/flags:use_x86_isa_is_true": ["//src/arch/x86/linux:fs_workload"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_x86_isa_is_true": ["//src/arch/x86/linux:se_workload"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_x86_isa_is_true": ["//src/arch/x86/linux:syscall_tbl32"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_x86_isa_is_true": ["//src/arch/x86/linux:syscall_tbl64"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_x86_isa_is_true": ["//src/arch/x86/linux:syscalls"],
        "//conditions:default": [],
    }) + [
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "fs_workload",
    srcs = ["fs_workload.cc"],
    hdrs = ["fs_workload.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/x86/linux",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/x86:fs_workload",
        "//src/base:trace",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/generated/params:X86FsLinux",
        "//src/mem:workaround_that_cuts_circular_dependency__port_proxy",
        "//src/sim:byteswap",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ] + select({
        "//src/generated/flags:use_x86_isa_is_true": ["//src/arch/x86/bios:e820"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_x86_isa_is_true": ["//src/arch/x86/regs:int"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "se_workload",
    srcs = ["se_workload.cc"],
    hdrs = ["se_workload.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/x86/linux",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/x86:page_size",
        "//src/arch/x86:process",
        "//src/arch/x86:remote_gdb",
        "//src/arch/x86:se_workload",
        "//src/arch/x86/linux",
        "//src/arch/x86/regs:misc",
        "//src/base:trace",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/generated/params:X86EmuLinux",
        "//src/kern/linux",
        "//src/mem:workaround_that_cuts_circular_dependency__se_translating_port_proxy",
        "//src/sim:se_workload",
        "//src/sim:syscall_abi",
        "//src/sim:syscall_desc",
        "//src/sim:syscall_emul",
        "//src/sim:workaround_that_cuts_circular_dependency__process",
    ] + select({
        "//src/generated/flags:use_x86_isa_is_true": ["//src/arch/x86/regs:int"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "syscall_tbl32",
    srcs = ["syscall_tbl32.cc"],
    hdrs = [],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/x86/linux",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/x86/linux",
        "//src/sim:syscall_emul",
    ] + select({
        "//src/generated/flags:use_x86_isa_is_true": ["//src/arch/x86/linux:se_workload"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_x86_isa_is_true": ["//src/arch/x86/linux:syscalls"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "syscall_tbl64",
    srcs = ["syscall_tbl64.cc"],
    hdrs = [],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/x86/linux",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/x86/linux",
        "//src/sim:syscall_emul",
    ] + select({
        "//src/generated/flags:use_x86_isa_is_true": ["//src/arch/x86/linux:se_workload"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_x86_isa_is_true": ["//src/arch/x86/linux:syscalls"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "syscalls",
    srcs = ["syscalls.cc"],
    hdrs = ["syscalls.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/x86/linux",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/x86:process",
        "//src/arch/x86/linux",
        "//src/arch/x86/regs:misc",
        "//src/base:bitunion",
        "//src/base:trace",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/kern/linux",
        "//src/mem:workaround_that_cuts_circular_dependency__se_translating_port_proxy",
        "//src/sim:se_workload",
        "//src/sim:syscall_desc",
        "//src/sim:syscall_emul",
        "//src/sim:workaround_that_cuts_circular_dependency__process",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "linux",
    srcs = [],
    hdrs = ["linux.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/x86/linux",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/x86:utility",
        "//src/arch/x86/regs:int",
        "//src/arch/x86/regs:misc",
        "//src/base:compiler",
        "//src/kern/linux",
        "//src/kern/linux:flag_tables",
        "//src/sim:guest_abi",
        "//src/sim:syscall_return",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
