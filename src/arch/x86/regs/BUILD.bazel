# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
    ] + select({
        "//src/generated/flags:use_x86_isa_is_true": ["//src/arch/x86/regs:float"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_x86_isa_is_true": ["//src/arch/x86/regs:int"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_x86_isa_is_true": ["//src/arch/x86/regs:msr"],
        "//conditions:default": [],
    }) + [
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "float",
    srcs = ["float.cc"],
    hdrs = ["float.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/x86/regs",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/x86:isa",
        "//src/arch/x86:x86_traits",
        "//src/arch/x86/regs:misc",
        "//src/base:bitunion",
        "//src/cpu:reg_class",
        "//src/generated/debug:FloatRegs",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "int",
    srcs = ["int.cc"],
    hdrs = ["int.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/x86/regs",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/x86:x86_traits",
        "//src/base:bitunion",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/cpu:reg_class",
        "//src/generated/debug:IntRegs",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "msr",
    srcs = ["msr.cc"],
    hdrs = ["msr.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/x86/regs",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/x86/regs:misc",
        "//src/base:types",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "apic",
    srcs = [],
    hdrs = ["apic.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/x86/regs",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/base:bitunion"],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "ccr",
    srcs = [],
    hdrs = ["ccr.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/x86/regs",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/cpu:reg_class",
        "//src/generated/debug:CCRegs",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "misc",
    srcs = [],
    hdrs = ["misc.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/x86/regs",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/x86:x86_traits",
        "//src/arch/x86/regs:segment",
        "//src/base:bitunion",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/cpu:reg_class",
        "//src/generated/debug:MiscRegs",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "segment",
    srcs = [],
    hdrs = ["segment.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "arch/x86/regs",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
