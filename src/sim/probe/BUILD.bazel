# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
        "//src/sim/probe",
        "//src/sim/probe:probe_listener_object",
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    macro_sim_object(
        "Probe.py",
        enums = [],
        sim_objects = ["ProbeListenerObject"],
    ),
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "probe",
    srcs = ["probe.cc"],
    hdrs = ["probe.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "sim/probe",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:named",
        "//src/base:trace",
        "//src/generated/debug:ProbeVerbose",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "probe_listener_object",
    srcs = ["probe_listener_object.cc"],
    hdrs = ["probe_listener_object.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "sim/probe",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/generated/params:ProbeListenerObject",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
        "//src/sim/probe:workaround_that_cuts_circular_dependency__probe",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "mem",
    srcs = [],
    hdrs = ["mem.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "sim/probe",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/sim/probe:workaround_that_cuts_circular_dependency__probe",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "pmu",
    srcs = [],
    hdrs = ["pmu.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "sim/probe",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/sim/probe:workaround_that_cuts_circular_dependency__probe"],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "workaround_that_cuts_circular_dependency__probe",
    srcs = [],
    hdrs = ["probe.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "sim/probe",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:named",
        "//src/base:trace",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
