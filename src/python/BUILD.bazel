# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
        "//src/python:embedded",
        "//src/python:importer",
        "//src/python/pybind11:core",
        "//src/python/pybind11:debug",
        "//src/python/pybind11:event",
        "//src/python/pybind11:object_file",
        "//src/python/pybind11:stats",
        ":m5_importer_code_pyo",
        "//src/python/m5:lib",
        "//src/python/pybind11:lib",
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        "//src/python/m5:sim_objects",
        "//src/python/pybind11:sim_objects",
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "embedded",
    srcs = ["embedded.cc"],
    hdrs = ["embedded.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "python",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["@pybind11//:lib"],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "importer",
    srcs = ["importer.cc"],
    hdrs = [],
    copts = COMMON_COPTS + [],
    include_prefix = "python",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/python:embedded",
        "//src/python:m5_importer_code_pyo",
        "//src/python:pybind_init",
        "@pybind11//:lib",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "pybind_init",
    srcs = [],
    hdrs = ["pybind_init.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "python",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["@pybind11//:lib"],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_binary(
    name = "gem5py",
    srcs = [
        "gem5py.cc",
    ],
    copts = COMMON_COPTS,
    linkopts = COMMON_LINKOPTS,
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["@pybind11//:lib"],
)

cc_binary(
    name = "gem5py_m5",
    srcs = [
        "gem5py_m5.cc",
    ],
    copts = COMMON_COPTS,
    linkopts = COMMON_LINKOPTS,
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        ":importer",
        ":m5_importer_code_pyo",
        "//src/arch:sim_objects",
        "//src/base:sim_objects",
        "//src/cpu:sim_objects",
        "//src/dev:sim_objects",
        "//src/kern:sim_objects",
        "//src/learning_gem5:sim_objects",
        "//src/mem:sim_objects",
        "//src/proto:sim_objects",
        "//src/python:sim_objects",
        "//src/python/m5:sim_objects_lib",
        "//src/sim:sim_objects",
        "//src/sst:sim_objects",
        "@pybind11//:lib",
    ],
)

gem5_py_source(
    src = "importer.py",
    module_prefix = "",
    name_o = "importer_py_o",
    name_pyo = "importer_py_pyo",
)

genrule(
    name = "gen_m5_importer_code",
    srcs = ["importer.py"],
    outs = [
        "m5ImporterCode.cc",
        "m5ImporterCode.hh",
    ],
    cmd = " ".join([
        "$(execpath //build_tools/bazel/hermetic_wrapper:scons_blob)",
        "\"$<\"",
        "m5ImporterCode",
        "\"$(RULEDIR)\"",
    ]) + " > /dev/null",
    tools = [
        "//build_tools:modules",
        "//build_tools/bazel/hermetic_wrapper:scons_blob",
        "//site_scons:modules",
        "//src/python/m5:modules",
    ],
)

cc_library(
    name = "m5_importer_code_pyo",
    srcs = ["m5ImporterCode.cc"],
    hdrs = ["m5ImporterCode.hh"],
    include_prefix = "python",
)
# TARGET_GOES_HERE.END
