# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
        "//src/dev/virtio:base",
        "//src/dev/virtio:block",
        "//src/dev/virtio:console",
        "//src/dev/virtio:fs9p",
        "//src/dev/virtio:pci",
        "//src/dev/virtio:rng",
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    macro_sim_object(
        "VirtIO9P.py",
        enums = [],
        sim_objects = [
            "VirtIO9PBase",
            "VirtIO9PProxy",
            "VirtIO9PDiod",
            "VirtIO9PSocket",
        ],
    ),
    macro_sim_object(
        "VirtIOBlock.py",
        enums = [],
        sim_objects = ["VirtIOBlock"],
    ),
    macro_sim_object(
        "VirtIOConsole.py",
        enums = [],
        sim_objects = ["VirtIOConsole"],
    ),
    macro_sim_object(
        "VirtIO.py",
        enums = [],
        sim_objects = [
            "VirtIODeviceBase",
            "VirtIODummyDevice",
            "PciVirtIO",
        ],
    ),
    macro_sim_object(
        "VirtIORng.py",
        enums = [],
        sim_objects = ["VirtIORng"],
    ),
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "base",
    srcs = ["base.cc"],
    hdrs = ["base.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/virtio",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:bitunion",
        "//src/base:compiler",
        "//src/base:trace",
        "//src/base:types",
        "//src/dev/virtio:virtio_ring",
        "//src/generated/debug:VIO",
        "//src/generated/params:VirtIODeviceBase",
        "//src/generated/params:VirtIODummyDevice",
        "//src/mem:workaround_that_cuts_circular_dependency__port_proxy",
        "//src/sim:serialize",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "block",
    srcs = ["block.cc"],
    hdrs = ["block.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/virtio",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/dev/storage:disk_image",
        "//src/dev/virtio:base",
        "//src/generated/debug:VIOBlock",
        "//src/generated/params:VirtIOBlock",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "console",
    srcs = ["console.cc"],
    hdrs = ["console.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/virtio",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/dev/serial",
        "//src/dev/virtio:base",
        "//src/generated/debug:VIOConsole",
        "//src/generated/params:VirtIOConsole",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "fs9p",
    srcs = ["fs9p.cc"],
    hdrs = ["fs9p.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/virtio",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:callback",
        "//src/base:compiler",
        "//src/base:output",
        "//src/base:pollevent",
        "//src/dev/virtio:base",
        "//src/generated/debug:VIO9P",
        "//src/generated/debug:VIO9PData",
        "//src/generated/params:VirtIO9PBase",
        "//src/generated/params:VirtIO9PDiod",
        "//src/generated/params:VirtIO9PProxy",
        "//src/generated/params:VirtIO9PSocket",
        "//src/sim:core",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "pci",
    srcs = ["pci.cc"],
    hdrs = ["pci.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/virtio",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:bitfield",
        "//src/base:compiler",
        "//src/base:workaround_that_cuts_circular_dependency__statistics",
        "//src/dev/pci:workaround_that_cuts_circular_dependency__device",
        "//src/dev/virtio:base",
        "//src/generated/debug:VIOIface",
        "//src/generated/params:PciVirtIO",
        "//src/mem:packet_access",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "rng",
    srcs = ["rng.cc"],
    hdrs = ["rng.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/virtio",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:random",
        "//src/dev/virtio:base",
        "//src/generated/debug:VIORng",
        "//src/generated/params:VirtIORng",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "virtio_ring",
    srcs = [],
    hdrs = ["virtio_ring.h"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/virtio",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
