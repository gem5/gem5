# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/dev/arm/css:mhu"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/dev/arm/css:scmi_platform"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/dev/arm/css:scmi_protocols"],
        "//conditions:default": [],
    }) + [
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
] + select({
    "//src/generated/flags:use_arm_isa_is_true": [
        macro_sim_object(
            "MHU.py",
            enums = [],
            sim_objects = [
                "MhuDoorbell",
                "Scp2ApDoorbell",
                "Ap2ScpDoorbell",
                "MHU",
            ],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_arm_isa_is_true": [
        macro_sim_object(
            "Scmi.py",
            enums = [],
            sim_objects = [
                "ScmiChannel",
                "ScmiAgentChannel",
                "ScmiPlatformChannel",
                "ScmiCommunication",
                "ScmiPlatform",
            ],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_arm_isa_is_true": [
        macro_sim_object(
            "Scp.py",
            enums = [],
            sim_objects = ["Scp"],
        ),
    ],
    "//conditions:default": [],
}) + [
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "mhu",
    srcs = ["mhu.cc"],
    hdrs = ["mhu.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/arm/css",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/dev:io_device",
        "//src/dev/arm:doorbell",
        "//src/dev/arm/css:scp",
        "//src/generated/debug:MHU",
        "//src/generated/params:Ap2ScpDoorbell",
        "//src/generated/params:MHU",
        "//src/generated/params:Scp2ApDoorbell",
        "//src/mem:packet_access",
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/dev/arm:base_gic"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "scmi_platform",
    srcs = ["scmi_platform.cc"],
    hdrs = ["scmi_platform.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/arm/css",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:bitunion",
        "//src/dev:dma_device",
        "//src/dev/arm:doorbell",
        "//src/dev/arm/css:scp",
        "//src/generated/debug:SCMI",
        "//src/generated/params:ScmiPlatform",
        "//src/mem:packet_access",
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/dev/arm/css:scmi_protocols"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "scmi_protocols",
    srcs = ["scmi_protocols.cc"],
    hdrs = ["scmi_protocols.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/arm/css",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/generated/debug:SCMI",
    ] + select({
        "//src/generated/flags:use_arm_isa_is_true": ["//src/dev/arm/css:scmi_platform"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "scp",
    srcs = [],
    hdrs = ["scp.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/arm/css",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/sim:workaround_that_cuts_circular_dependency__clocked_object"],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
