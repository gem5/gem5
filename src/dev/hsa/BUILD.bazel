# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/dev/hsa:hsa_packet_processor"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/dev/hsa:hw_scheduler"],
        "//conditions:default": [],
    }) + [
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
] + select({
    "//src/generated/flags:build_gpu_is_true": [
        macro_sim_object(
            "HSADevice.py",
            enums = [],
            sim_objects = ["HSAPacketProcessor"],
        ),
    ],
    "//conditions:default": [],
}) + [
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "hsa_packet_processor",
    srcs = ["hsa_packet_processor.cc"],
    hdrs = ["hsa_packet_processor.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/hsa",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:chunk_generator",
        "//src/base:compiler",
        "//src/base:trace",
        "//src/base:types",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/dev:dma_device",
        "//src/dev:dma_virt_device",
        "//src/dev/hsa",
        "//src/dev/hsa:hsa_packet",
        "//src/dev/hsa:hsa_queue",
        "//src/generated/debug:HSAPacketProcessor",
        "//src/generated/enums:GfxVersion",
        "//src/generated/params:HSAPacketProcessor",
        "//src/mem:packet_access",
        "//src/mem:workaround_that_cuts_circular_dependency__page_table",
        "//src/sim:eventq",
        "//src/sim:full_system",
        "//src/sim:proxy_ptr",
        "//src/sim:workaround_that_cuts_circular_dependency__process",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/dev/hsa:hw_scheduler"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/gpu-compute:gpu_command_processor"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_x86_isa_is_true": ["//src/dev/amdgpu:amdgpu_device"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "hw_scheduler",
    srcs = ["hw_scheduler.cc"],
    hdrs = ["hw_scheduler.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/hsa",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:trace",
        "//src/base:types",
        "//src/generated/debug:HSAPacketProcessor",
        "//src/generated/enums:GfxVersion",
        "//src/sim:cur_tick",
        "//src/sim:eventq",
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/dev/hsa:hsa_packet_processor"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "hsa",
    srcs = [],
    hdrs = ["hsa.h"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/hsa",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "hsa_packet",
    srcs = [],
    hdrs = ["hsa_packet.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/hsa",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "hsa_queue",
    srcs = [],
    hdrs = ["hsa_queue.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/hsa",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "hsa_signal",
    srcs = [],
    hdrs = ["hsa_signal.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/hsa",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "kfd_event_defines",
    srcs = [],
    hdrs = ["kfd_event_defines.h"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/hsa",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/dev/hsa:kfd_ioctl"],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "kfd_ioctl",
    srcs = [],
    hdrs = ["kfd_ioctl.h"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/hsa",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
