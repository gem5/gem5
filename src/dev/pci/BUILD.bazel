# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
        "//src/dev/pci:copy_engine",
        "//src/dev/pci:device",
        "//src/dev/pci:host",
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    macro_sim_object(
        "CopyEngine.py",
        enums = [],
        sim_objects = ["CopyEngine"],
    ),
    macro_sim_object(
        "PciDevice.py",
        enums = [],
        sim_objects = [
            "PciBar",
            "PciBarNone",
            "PciIoBar",
            "PciLegacyIoBar",
            "PciMemBar",
            "PciMemUpperBar",
            "PciDevice",
            "PciEndpoint",
            "PciBridge",
        ],
    ),
    macro_sim_object(
        "PciHost.py",
        enums = [],
        sim_objects = [
            "PciHost",
            "GenericPciHost",
        ],
    ),
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "copy_engine",
    srcs = ["copy_engine.cc"],
    hdrs = ["copy_engine.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/pci",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__statistics",
        "//src/dev/pci:copy_engine_defs",
        "//src/dev/pci:workaround_that_cuts_circular_dependency__device",
        "//src/generated/debug:DMACopyEngine",
        "//src/generated/debug:Drain",
        "//src/generated/params:CopyEngine",
        "//src/mem:packet_access",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/sim:drain",
        "//src/sim:eventq",
        "//src/sim:stats",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "device",
    srcs = ["device.cc"],
    hdrs = ["device.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/pci",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:inifile",
        "//src/base:intmath",
        "//src/base:str",
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/dev:dma_device",
        "//src/dev/pci:pcireg",
        "//src/dev/pci:workaround_that_cuts_circular_dependency__host",
        "//src/generated/debug:PciBridge",
        "//src/generated/debug:PciDevice",
        "//src/generated/debug:PciEndpoint",
        "//src/generated/params:PciBar",
        "//src/generated/params:PciBarNone",
        "//src/generated/params:PciBridge",
        "//src/generated/params:PciDevice",
        "//src/generated/params:PciEndpoint",
        "//src/generated/params:PciIoBar",
        "//src/generated/params:PciLegacyIoBar",
        "//src/generated/params:PciMemBar",
        "//src/generated/params:PciMemUpperBar",
        "//src/mem:packet_access",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/sim:byteswap",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "host",
    srcs = ["host.cc"],
    hdrs = ["host.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/pci",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/dev:io_device",
        "//src/dev:platform",
        "//src/dev/pci:types",
        "//src/dev/pci:workaround_that_cuts_circular_dependency__device",
        "//src/generated/debug:PciHost",
        "//src/generated/params:GenericPciHost",
        "//src/generated/params:PciHost",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "copy_engine_defs",
    srcs = [],
    hdrs = ["copy_engine_defs.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/pci",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:bitfield",
        "//src/base:compiler",
        "//src/sim:serialize",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "workaround_that_cuts_circular_dependency__device",
    srcs = [],
    hdrs = ["device.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/pci",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/dev:dma_device",
        "//src/dev/pci:pcireg",
        "//src/dev/pci:workaround_that_cuts_circular_dependency__host",
        "//src/generated/params:PciBar",
        "//src/generated/params:PciBarNone",
        "//src/generated/params:PciBridge",
        "//src/generated/params:PciDevice",
        "//src/generated/params:PciEndpoint",
        "//src/generated/params:PciIoBar",
        "//src/generated/params:PciLegacyIoBar",
        "//src/generated/params:PciMemBar",
        "//src/generated/params:PciMemUpperBar",
        "//src/sim:byteswap",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "workaround_that_cuts_circular_dependency__host",
    srcs = [],
    hdrs = ["host.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/pci",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/dev:io_device",
        "//src/dev/pci:types",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "pcireg",
    srcs = [],
    hdrs = ["pcireg.h"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/pci",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:bitfield",
        "//src/base:bitunion",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "types",
    srcs = [],
    hdrs = ["types.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/pci",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
