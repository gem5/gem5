# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
        "//src/dev/serial",
        "//src/dev/serial:simple",
        "//src/dev/serial:terminal",
        "//src/dev/serial:uart",
        "//src/dev/serial:uart8250",
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    macro_sim_object(
        "Serial.py",
        enums = [],
        sim_objects = [
            "SerialDevice",
            "SerialNullDevice",
        ],
    ),
    macro_sim_object(
        "Uart.py",
        enums = [],
        sim_objects = [
            "Uart",
            "SimpleUart",
            "Uart8250",
        ],
    ),
    macro_sim_object(
        "Terminal.py",
        enums = ["TerminalDump"],
        sim_objects = ["Terminal"],
    ),
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "serial",
    srcs = ["serial.cc"],
    hdrs = ["serial.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/serial",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/generated/params:SerialDevice",
        "//src/generated/params:SerialNullDevice",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "simple",
    srcs = ["simple.cc"],
    hdrs = ["simple.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/serial",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/dev/serial:uart",
        "//src/generated/params:SimpleUart",
        "//src/mem:packet_access",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/sim:sim_exit",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "terminal",
    srcs = ["terminal.cc"],
    hdrs = ["terminal.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/serial",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:atomicio",
        "//src/base:callback",
        "//src/base:circlebuf",
        "//src/base:output",
        "//src/base:pollevent",
        "//src/base:socket",
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/dev:platform",
        "//src/dev/serial",
        "//src/dev/serial:uart",
        "//src/generated/debug:Terminal",
        "//src/generated/debug:TerminalVerbose",
        "//src/generated/params:Terminal",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "uart",
    srcs = ["uart.cc"],
    hdrs = ["uart.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/serial",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:callback",
        "//src/dev:io_device",
        "//src/dev/serial",
        "//src/generated/params:Uart",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "uart8250",
    srcs = ["uart8250.cc"],
    hdrs = ["uart8250.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/serial",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:bitunion",
        "//src/base:inifile",
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/dev:io_device",
        "//src/dev:platform",
        "//src/dev:reg_bank",
        "//src/dev/serial:uart",
        "//src/generated/debug:Uart",
        "//src/generated/params:Uart8250",
        "//src/mem:packet_access",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/sim:serialize",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
