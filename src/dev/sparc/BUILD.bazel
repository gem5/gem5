# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
    ] + select({
        "//src/generated/flags:use_sparc_isa_is_true": ["//src/dev/sparc:dtod"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_sparc_isa_is_true": ["//src/dev/sparc:iob"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_sparc_isa_is_true": ["//src/dev/sparc:mm_disk"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_sparc_isa_is_true": ["//src/dev/sparc:t1000"],
        "//conditions:default": [],
    }) + [
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
] + select({
    "//src/generated/flags:use_sparc_isa_is_true": [
        macro_sim_object(
            "T1000.py",
            enums = [],
            sim_objects = [
                "MmDisk",
                "DumbTOD",
                "Iob",
                "T1000",
            ],
        ),
    ],
    "//conditions:default": [],
}) + [
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "dtod",
    srcs = ["dtod.cc"],
    hdrs = ["dtod.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/sparc",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:time",
        "//src/base:trace",
        "//src/dev:io_device",
        "//src/dev:platform",
        "//src/generated/params:DumbTOD",
        "//src/mem:packet_access",
        "//src/mem:port",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "iob",
    srcs = ["iob.cc"],
    hdrs = ["iob.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/sparc",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/sparc:interrupts",
        "//src/base:bitfield",
        "//src/base:trace",
        "//src/cpu:workaround_that_cuts_circular_dependency__base",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/dev:io_device",
        "//src/dev:platform",
        "//src/generated/debug:Iob",
        "//src/generated/params:Iob",
        "//src/mem:packet_access",
        "//src/mem:port",
        "//src/sim:faults",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ] + select({
        "//src/generated/flags:use_sparc_isa_is_true": ["//src/arch/sparc:faults"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "mm_disk",
    srcs = ["mm_disk.cc"],
    hdrs = ["mm_disk.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/sparc",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:trace",
        "//src/dev:io_device",
        "//src/dev:platform",
        "//src/dev/storage:disk_image",
        "//src/generated/debug:IdeDisk",
        "//src/generated/params:MmDisk",
        "//src/mem:packet_access",
        "//src/mem:port",
        "//src/sim:byteswap",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "t1000",
    srcs = ["t1000.cc"],
    hdrs = ["t1000.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/sparc",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/dev:platform",
        "//src/generated/params:T1000",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
