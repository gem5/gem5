# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/dev/lupio:lupio_blk"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/dev/lupio:lupio_ipi"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/dev/lupio:lupio_pic"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/dev/lupio:lupio_rng"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/dev/lupio:lupio_rtc"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/dev/lupio:lupio_sys"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/dev/lupio:lupio_tmr"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/dev/lupio:lupio_tty"],
        "//conditions:default": [],
    }) + [
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
] + select({
    "//src/generated/flags:use_riscv_isa_is_true": [
        macro_sim_object(
            "LupioBLK.py",
            enums = [],
            sim_objects = ["LupioBLK"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_riscv_isa_is_true": [
        macro_sim_object(
            "LupioIPI.py",
            enums = [],
            sim_objects = ["LupioIPI"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_riscv_isa_is_true": [
        macro_sim_object(
            "LupioPIC.py",
            enums = [],
            sim_objects = ["LupioPIC"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_riscv_isa_is_true": [
        macro_sim_object(
            "LupioRNG.py",
            enums = [],
            sim_objects = ["LupioRNG"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_riscv_isa_is_true": [
        macro_sim_object(
            "LupioRTC.py",
            enums = [],
            sim_objects = ["LupioRTC"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_riscv_isa_is_true": [
        macro_sim_object(
            "LupioSYS.py",
            enums = [],
            sim_objects = ["LupioSYS"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_riscv_isa_is_true": [
        macro_sim_object(
            "LupioTMR.py",
            enums = [],
            sim_objects = ["LupioTMR"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_riscv_isa_is_true": [
        macro_sim_object(
            "LupioTTY.py",
            enums = [],
            sim_objects = ["LupioTTY"],
        ),
    ],
    "//conditions:default": [],
}) + [
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "lupio_blk",
    srcs = ["lupio_blk.cc"],
    hdrs = ["lupio_blk.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/lupio",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/dev:dma_device",
        "//src/dev:io_device",
        "//src/dev:platform",
        "//src/dev/storage:disk_image",
        "//src/generated/debug:LupioBLK",
        "//src/generated/params:LupioBLK",
        "//src/mem:packet_access",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/dev/lupio:lupio_pic"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "lupio_ipi",
    srcs = ["lupio_ipi.cc"],
    hdrs = ["lupio_ipi.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/lupio",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/cpu:workaround_that_cuts_circular_dependency__base",
        "//src/dev:io_device",
        "//src/dev:platform",
        "//src/generated/debug:LupioIPI",
        "//src/generated/params:LupioIPI",
        "//src/mem:packet_access",
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv:interrupts"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "lupio_pic",
    srcs = ["lupio_pic.cc"],
    hdrs = ["lupio_pic.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/lupio",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/cpu:workaround_that_cuts_circular_dependency__base",
        "//src/dev:io_device",
        "//src/dev:platform",
        "//src/generated/debug:LupioPIC",
        "//src/generated/params:LupioPIC",
        "//src/mem:packet_access",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv:interrupts"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "lupio_rng",
    srcs = ["lupio_rng.cc"],
    hdrs = ["lupio_rng.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/lupio",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/dev:io_device",
        "//src/generated/debug:LupioRNG",
        "//src/generated/params:LupioRNG",
        "//src/mem:packet_access",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "lupio_rtc",
    srcs = ["lupio_rtc.cc"],
    hdrs = ["lupio_rtc.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/lupio",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:time",
        "//src/dev:io_device",
        "//src/generated/debug:LupioRTC",
        "//src/generated/params:LupioRTC",
        "//src/mem:packet_access",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "lupio_sys",
    srcs = ["lupio_sys.cc"],
    hdrs = ["lupio_sys.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/lupio",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/dev:io_device",
        "//src/dev:platform",
        "//src/generated/debug:LupioSYS",
        "//src/generated/params:LupioSYS",
        "//src/mem:packet_access",
        "//src/sim:sim_exit",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "lupio_tmr",
    srcs = ["lupio_tmr.cc"],
    hdrs = ["lupio_tmr.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/lupio",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/cpu:workaround_that_cuts_circular_dependency__base",
        "//src/dev:io_device",
        "//src/dev:platform",
        "//src/generated/debug:LupioTMR",
        "//src/generated/params:LupioTMR",
        "//src/mem:packet_access",
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv:interrupts"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "lupio_tty",
    srcs = ["lupio_tty.cc"],
    hdrs = ["lupio_tty.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/lupio",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:trace",
        "//src/dev:io_device",
        "//src/dev:platform",
        "//src/dev/serial",
        "//src/generated/debug:LupioTTY",
        "//src/generated/params:LupioTTY",
        "//src/mem:packet_access",
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/dev/lupio:lupio_pic"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
