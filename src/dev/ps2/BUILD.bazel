# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
        "//src/dev/ps2:device",
        "//src/dev/ps2:keyboard",
        "//src/dev/ps2:mouse",
        "//src/dev/ps2:touchkit",
        "//src/dev/ps2:types",
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    macro_sim_object(
        "PS2.py",
        enums = [],
        sim_objects = [
            "PS2Device",
            "PS2Keyboard",
            "PS2Mouse",
            "PS2TouchKit",
        ],
    ),
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "device",
    srcs = ["device.cc"],
    hdrs = ["device.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/ps2",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/dev/ps2:types",
        "//src/generated/debug:PS2",
        "//src/generated/params:PS2Device",
        "//src/sim:serialize",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "keyboard",
    srcs = ["keyboard.cc"],
    hdrs = ["keyboard.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/ps2",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/base/vnc:vncinput",
        "//src/dev/ps2:device",
        "//src/dev/ps2:types",
        "//src/generated/debug:PS2",
        "//src/generated/params:PS2Keyboard",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "mouse",
    srcs = ["mouse.cc"],
    hdrs = ["mouse.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/ps2",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:bitunion",
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/dev/ps2:device",
        "//src/dev/ps2:types",
        "//src/generated/debug:PS2",
        "//src/generated/params:PS2Mouse",
        "//src/sim:serialize",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "touchkit",
    srcs = ["touchkit.cc"],
    hdrs = ["touchkit.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/ps2",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/base/vnc:vncinput",
        "//src/dev/ps2:device",
        "//src/dev/ps2:mouse",
        "//src/dev/ps2:types",
        "//src/generated/debug:PS2",
        "//src/generated/params:PS2TouchKit",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "types",
    srcs = ["types.cc"],
    hdrs = ["types.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/ps2",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:bitunion",
        "//src/base:compiler",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "@x11keysym//:lib",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
