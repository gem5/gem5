# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
        "//src/dev/storage:disk_image",
        "//src/dev/storage:ide_ctrl",
        "//src/dev/storage:ide_disk",
        "//src/dev/storage:simple_disk",
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    macro_sim_object(
        "DiskImage.py",
        enums = [],
        sim_objects = [
            "DiskImage",
            "RawDiskImage",
            "CowDiskImage",
        ],
    ),
    macro_sim_object(
        "SimpleDisk.py",
        enums = [],
        sim_objects = ["SimpleDisk"],
    ),
    macro_sim_object(
        "Ide.py",
        enums = ["IdeID"],
        sim_objects = [
            "IdeDisk",
            "IdeController",
        ],
    ),
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "disk_image",
    srcs = ["disk_image.cc"],
    hdrs = ["disk_image.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/storage",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:callback",
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/generated/debug:DiskImageRead",
        "//src/generated/debug:DiskImageWrite",
        "//src/generated/params:CowDiskImage",
        "//src/generated/params:DiskImage",
        "//src/generated/params:RawDiskImage",
        "//src/sim:byteswap",
        "//src/sim:serialize",
        "//src/sim:sim_exit",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "ide_ctrl",
    srcs = ["ide_ctrl.cc"],
    hdrs = ["ide_ctrl.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/storage",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:bitunion",
        "//src/base:cprintf",
        "//src/dev:io_device",
        "//src/dev:reg_bank",
        "//src/dev/pci:workaround_that_cuts_circular_dependency__device",
        "//src/dev/storage:ide_disk",
        "//src/generated/debug:IdeCtrl",
        "//src/generated/params:IdeController",
        "//src/mem:packet_access",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/sim:byteswap",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "ide_disk",
    srcs = ["ide_disk.cc"],
    hdrs = ["ide_disk.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/storage",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:bitfield",
        "//src/base:chunk_generator",
        "//src/base:compiler",
        "//src/base:cprintf",
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__statistics",
        "//src/dev:io_device",
        "//src/dev/storage:disk_image",
        "//src/dev/storage:ide_atareg",
        "//src/dev/storage:ide_wdcreg",
        "//src/dev/storage:workaround_that_cuts_circular_dependency__ide_ctrl",
        "//src/generated/debug:IdeDisk",
        "//src/generated/params:IdeDisk",
        "//src/sim:cur_tick",
        "//src/sim:eventq",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "simple_disk",
    srcs = ["simple_disk.cc"],
    hdrs = ["simple_disk.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/storage",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/dev/storage:disk_image",
        "//src/generated/debug:SimpleDisk",
        "//src/generated/debug:SimpleDiskData",
        "//src/generated/params:SimpleDisk",
        "//src/mem:workaround_that_cuts_circular_dependency__port_proxy",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "ide_atareg",
    srcs = [],
    hdrs = ["ide_atareg.h"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/storage",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "workaround_that_cuts_circular_dependency__ide_ctrl",
    srcs = [],
    hdrs = ["ide_ctrl.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/storage",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:bitunion",
        "//src/dev:io_device",
        "//src/dev:reg_bank",
        "//src/dev/pci:workaround_that_cuts_circular_dependency__device",
        "//src/generated/params:IdeController",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "ide_wdcreg",
    srcs = [],
    hdrs = ["ide_wdcreg.h"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/storage",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
