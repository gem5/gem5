# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/dev/riscv:clint"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/dev/riscv:hifive"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/dev/riscv:lupv"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/dev/riscv:pci_host"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/dev/riscv:plic"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/dev/riscv:plic_device"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/dev/riscv:rtc"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/dev/riscv:vio_mmio"],
        "//conditions:default": [],
    }) + [
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
] + select({
    "//src/generated/flags:use_riscv_isa_is_true": [
        macro_sim_object(
            "Clint.py",
            enums = [],
            sim_objects = ["Clint"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_riscv_isa_is_true": [
        macro_sim_object(
            "HiFive.py",
            enums = [],
            sim_objects = [
                "HiFiveBase",
                "GenericRiscvPciHost",
            ],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_riscv_isa_is_true": [
        macro_sim_object(
            "LupV.py",
            enums = [],
            sim_objects = ["LupV"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_riscv_isa_is_true": [
        macro_sim_object(
            "Plic.py",
            enums = [],
            sim_objects = [
                "PlicBase",
                "Plic",
            ],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_riscv_isa_is_true": [
        macro_sim_object(
            "PlicDevice.py",
            enums = [],
            sim_objects = ["PlicIntDevice"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_riscv_isa_is_true": [
        macro_sim_object(
            "RiscvVirtIOMMIO.py",
            enums = [],
            sim_objects = ["RiscvMmioVirtIO"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_riscv_isa_is_true": [
        macro_sim_object(
            "RTC.py",
            enums = [],
            sim_objects = ["RiscvRTC"],
        ),
    ],
    "//conditions:default": [],
}) + [
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "clint",
    srcs = ["clint.cc"],
    hdrs = ["clint.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/riscv",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/cpu:workaround_that_cuts_circular_dependency__base",
        "//src/dev:intpin",
        "//src/dev:io_device",
        "//src/dev:mc146818",
        "//src/dev:reg_bank",
        "//src/generated/debug:Clint",
        "//src/generated/params:Clint",
        "//src/mem:packet_access",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv:interrupts"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "hifive",
    srcs = ["hifive.cc"],
    hdrs = ["hifive.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/riscv",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/dev:platform",
        "//src/generated/params:HiFiveBase",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/dev/riscv:clint"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/dev/riscv:plic"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "lupv",
    srcs = ["lupv.cc"],
    hdrs = ["lupv.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/riscv",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/dev:platform",
        "//src/generated/params:LupV",
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/dev/lupio:lupio_pic"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "pci_host",
    srcs = ["pci_host.cc"],
    hdrs = ["pci_host.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/riscv",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/dev/pci:workaround_that_cuts_circular_dependency__host",
        "//src/generated/params:GenericRiscvPciHost",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "plic",
    srcs = ["plic.cc"],
    hdrs = ["plic.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/riscv",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/cpu:workaround_that_cuts_circular_dependency__base",
        "//src/dev:io_device",
        "//src/dev:reg_bank",
        "//src/generated/debug:Plic",
        "//src/generated/params:Plic",
        "//src/generated/params:PlicBase",
        "//src/mem:packet_access",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/arch/riscv:interrupts"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "plic_device",
    srcs = ["plic_device.cc"],
    hdrs = ["plic_device.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/riscv",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/dev:io_device",
        "//src/dev:platform",
        "//src/generated/params:PlicIntDevice",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "rtc",
    srcs = ["rtc.cc"],
    hdrs = ["rtc.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/riscv",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/dev:intpin",
        "//src/dev:mc146818",
        "//src/generated/params:RiscvRTC",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "vio_mmio",
    srcs = ["vio_mmio.cc"],
    hdrs = ["vio_mmio.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "dev/riscv",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/dev/virtio:base",
        "//src/generated/debug:VirtIOMMIO",
        "//src/generated/params:RiscvMmioVirtIO",
        "//src/mem:packet_access",
    ] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/dev/riscv:hifive"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_riscv_isa_is_true": ["//src/dev/riscv:plic_device"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
