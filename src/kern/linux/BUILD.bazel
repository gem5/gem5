# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "events",
    srcs = ["events.cc"],
    hdrs = ["events.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "kern/linux",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:output",
        "//src/base:trace",
        "//src/cpu:workaround_that_cuts_circular_dependency__base",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/generated/debug:DebugPrintf",
        "//src/generated/enums:KernelPanicOopsBehaviour",
        "//src/kern:system_events",
        "//src/kern/linux:helpers",
        "//src/kern/linux:printk",
        "//src/mem:workaround_that_cuts_circular_dependency__se_translating_port_proxy",
        "//src/sim:core",
        "//src/sim:guest_abi",
        "//src/sim:sim_exit",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "helpers",
    srcs = ["helpers.cc"],
    hdrs = ["helpers.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "kern/linux",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base/loader:workaround_that_cuts_circular_dependency__object_file",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/mem:translating_port_proxy",
        "//src/mem:workaround_that_cuts_circular_dependency__port_proxy",
        "//src/sim:byteswap",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "linux",
    srcs = ["linux.cc"],
    hdrs = ["linux.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "kern/linux",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:random",
        "//src/base:types",
        "//src/cpu:workaround_that_cuts_circular_dependency__base",
        "//src/generated/debug:SyscallVerbose",
        "//src/kern:operatingsystem",
        "//src/sim:se_workload",
        "//src/sim:workaround_that_cuts_circular_dependency__mem_state",
        "//src/sim:workaround_that_cuts_circular_dependency__process",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
        "//src/sim:workaround_that_cuts_circular_dependency__vma",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "printk",
    srcs = ["printk.cc"],
    hdrs = ["printk.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "kern/linux",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:types",
        "//src/cpu:workaround_that_cuts_circular_dependency__thread_context",
        "//src/mem:translating_port_proxy",
        "//src/mem:workaround_that_cuts_circular_dependency__port_proxy",
        "//src/sim:guest_abi",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "flag_tables",
    srcs = [],
    hdrs = ["flag_tables.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "kern/linux",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
