# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
        "//src/base/stats:group",
        "//src/base/stats:info",
        "//src/base/stats:storage",
        "//src/base/stats:text",
    ] + select({
        "//src/generated/flags:have_hdf5_is_true": ["//src/base/stats:hdf5"],
        "//conditions:default": [],
    }) + [
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "group",
    srcs = ["group.cc"],
    hdrs = ["group.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "base/stats",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:named",
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/base/stats:info",
        "//src/base/stats:units",
        "//src/generated/debug:Stats",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "info",
    srcs = ["info.cc"],
    hdrs = ["info.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "base/stats",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:cprintf",
        "//src/base:debug",
        "//src/base:flags",
        "//src/base:str",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/base/stats:storage",
        "//src/base/stats:types",
        "//src/base/stats:units",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "storage",
    srcs = ["storage.cc"],
    hdrs = ["storage.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "base/stats",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:cast",
        "//src/base:compiler",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/base/stats:types",
        "//src/sim:cur_tick",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "text",
    srcs = ["text.cc"],
    hdrs = ["text.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "base/stats",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:cast",
        "//src/base:compiler",
        "//src/base:output",
        "//src/base:str",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/base/stats:info",
        "//src/base/stats:output",
        "//src/base/stats:types",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "hdf5",
    srcs = ["hdf5.cc"],
    hdrs = ["hdf5.hh"],
    copts = COMMON_COPTS + ["-Wno-deprecated-copy"],
    include_prefix = "base/stats",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:output",
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/base/stats:info",
        "//src/base/stats:output",
        "//src/base/stats:types",
        "//src/generated/debug:Stats",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "output",
    srcs = [],
    hdrs = ["output.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "base/stats",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/base:compiler"],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "types",
    srcs = [],
    hdrs = ["types.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "base/stats",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:types",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "units",
    srcs = [],
    hdrs = ["units.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "base/stats",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:cprintf",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
