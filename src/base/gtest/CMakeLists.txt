# src/base/gtest/CMakeLists.txt
# Converted from src/base/gtest/SConscript
#
# These are GTest support libraries (not test executables).
# They are linked into GTest test binaries, NOT into the main gem5 library.
# (tag_gem5_lib=False in the SCons build)
#
# The actual compilation happens in Gem5Targets.cmake as part of the
# gem5_ext_gtest library or test executables.
