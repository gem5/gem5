# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
        "//src/base/vnc:vncinput",
        "//src/base/vnc:vncserver",
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    macro_sim_object(
        "Vnc.py",
        enums = [],
        sim_objects = [
            "VncInput",
            "VncServer",
        ],
    ),
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "vncinput",
    srcs = ["vncinput.cc"],
    hdrs = ["vncinput.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "base/vnc",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:output",
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__imgwriter",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/generated/debug:VNC",
        "//src/generated/params:VncInput",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "vncserver",
    srcs = ["vncserver.cc"],
    hdrs = ["vncserver.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "base/vnc",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:atomicio",
        "//src/base:circlebuf",
        "//src/base:compiler",
        "//src/base:output",
        "//src/base:pollevent",
        "//src/base:socket",
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/base/vnc:vncinput",
        "//src/generated/debug:VNC",
        "//src/generated/params:VncServer",
        "//src/sim:byteswap",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
