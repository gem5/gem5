# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
        "//src/base/loader:dtb_file",
        "//src/base/loader:elf_object",
        "//src/base/loader:image_file_data",
        "//src/base/loader:memory_image",
        "//src/base/loader:object_file",
        "//src/base/loader:symtab",
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "dtb_file",
    srcs = ["dtb_file.cc"],
    hdrs = ["dtb_file.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "base/loader",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base/loader:image_file",
        "//src/sim:byteswap",
        "@libfdt//:lib",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "elf_object",
    srcs = ["elf_object.cc"],
    hdrs = ["elf_object.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "base/loader",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:bitfield",
        "//src/base:compiler",
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/base/loader:symtab",
        "//src/base/loader:workaround_that_cuts_circular_dependency__object_file",
        "//src/generated/debug:Loader",
        "//src/sim:byteswap",
        "@libelf//:lib",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "image_file_data",
    srcs = ["image_file_data.cc"],
    hdrs = ["image_file_data.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "base/loader",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "memory_image",
    srcs = ["memory_image.cc"],
    hdrs = ["memory_image.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "base/loader",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:types",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/base/loader:image_file_data",
        "//src/mem:workaround_that_cuts_circular_dependency__port_proxy",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "object_file",
    srcs = ["object_file.cc"],
    hdrs = ["object_file.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "base/loader",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:types",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/base/loader:image_file",
        "//src/base/loader:image_file_data",
        "//src/base/loader:memory_image",
        "//src/base/loader:raw_image",
        "//src/base/loader:symtab",
        "//src/generated/enums:ByteOrder",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "symtab",
    srcs = ["symtab.cc"],
    hdrs = ["symtab.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "base/loader",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:str",
        "//src/base:types",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/sim:serialize",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "image_file",
    srcs = [],
    hdrs = ["image_file.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "base/loader",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base/loader:image_file_data",
        "//src/base/loader:memory_image",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "workaround_that_cuts_circular_dependency__object_file",
    srcs = [],
    hdrs = ["object_file.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "base/loader",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:types",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/base/loader:image_file",
        "//src/base/loader:image_file_data",
        "//src/base/loader:memory_image",
        "//src/base/loader:symtab",
        "//src/generated/enums:ByteOrder",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "raw_image",
    srcs = [],
    hdrs = ["raw_image.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "base/loader",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base/loader:workaround_that_cuts_circular_dependency__object_file",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
