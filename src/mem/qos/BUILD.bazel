# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
        "//src/mem/qos:mem_ctrl",
        "//src/mem/qos:mem_sink",
        "//src/mem/qos:policy",
        "//src/mem/qos:policy_fixed_prio",
        "//src/mem/qos:policy_pf",
        "//src/mem/qos:q_policy",
        "//src/mem/qos:turnaround_policy_ideal",
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    macro_sim_object(
        "QoSMemSinkCtrl.py",
        enums = [],
        sim_objects = ["QoSMemSinkCtrl"],
    ),
    macro_sim_object(
        "QoSMemSinkInterface.py",
        enums = [],
        sim_objects = ["QoSMemSinkInterface"],
    ),
    macro_sim_object(
        "QoSPolicy.py",
        enums = [],
        sim_objects = [
            "QoSPolicy",
            "QoSFixedPriorityPolicy",
            "QoSPropFairPolicy",
        ],
    ),
    macro_sim_object(
        "QoSTurnaround.py",
        enums = [],
        sim_objects = [
            "QoSTurnaroundPolicy",
            "QoSTurnaroundPolicyIdeal",
        ],
    ),
    macro_sim_object(
        "QoSMemCtrl.py",
        enums = ["QoSQPolicy"],
        sim_objects = ["QoSMemCtrl"],
    ),
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "mem_ctrl",
    srcs = ["mem_ctrl.cc"],
    hdrs = ["mem_ctrl.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/qos",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:trace",
        "//src/base:types",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/base:workaround_that_cuts_circular_dependency__statistics",
        "//src/generated/debug:QOS",
        "//src/generated/params:QoSMemCtrl",
        "//src/mem:request",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/mem/qos:policy",
        "//src/mem/qos:q_policy",
        "//src/mem/qos:turnaround_policy",
        "//src/sim:core",
        "//src/sim:cur_tick",
        "//src/sim:workaround_that_cuts_circular_dependency__clocked_object",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "mem_sink",
    srcs = ["mem_sink.cc"],
    hdrs = ["mem_sink.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/qos",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:trace",
        "//src/base:types",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/generated/debug:Drain",
        "//src/generated/debug:QOS",
        "//src/generated/params:QoSMemSinkCtrl",
        "//src/generated/params:QoSMemSinkInterface",
        "//src/mem:qport",
        "//src/mem:workaround_that_cuts_circular_dependency__abstract_mem",
        "//src/mem/qos:q_policy",
        "//src/mem/qos:workaround_that_cuts_circular_dependency__mem_ctrl",
        "//src/sim:eventq",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "policy",
    srcs = ["policy.cc"],
    hdrs = ["policy.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/qos",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/generated/debug:QOS",
        "//src/generated/params:QoSPolicy",
        "//src/mem:request",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/mem/qos:workaround_that_cuts_circular_dependency__mem_ctrl",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "policy_fixed_prio",
    srcs = ["policy_fixed_prio.cc"],
    hdrs = ["policy_fixed_prio.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/qos",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:trace",
        "//src/generated/debug:QOS",
        "//src/generated/params:QoSFixedPriorityPolicy",
        "//src/mem:request",
        "//src/mem/qos:policy",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "policy_pf",
    srcs = ["policy_pf.cc"],
    hdrs = ["policy_pf.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/qos",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/generated/params:QoSPropFairPolicy",
        "//src/mem:request",
        "//src/mem/qos:policy",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "q_policy",
    srcs = ["q_policy.cc"],
    hdrs = ["q_policy.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/qos",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/generated/debug:QOS",
        "//src/generated/enums:QoSQPolicy",
        "//src/generated/params:QoSMemCtrl",
        "//src/mem:request",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/mem/qos:workaround_that_cuts_circular_dependency__mem_ctrl",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "turnaround_policy_ideal",
    srcs = ["turnaround_policy_ideal.cc"],
    hdrs = ["turnaround_policy_ideal.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/qos",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:trace",
        "//src/generated/params:QoSTurnaroundPolicyIdeal",
        "//src/mem/qos:turnaround_policy",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "workaround_that_cuts_circular_dependency__mem_ctrl",
    srcs = [],
    hdrs = ["mem_ctrl.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/qos",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:trace",
        "//src/base:types",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/base:workaround_that_cuts_circular_dependency__statistics",
        "//src/generated/debug:QOS",
        "//src/generated/params:QoSMemCtrl",
        "//src/mem:request",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/sim:cur_tick",
        "//src/sim:workaround_that_cuts_circular_dependency__clocked_object",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "turnaround_policy",
    srcs = [],
    hdrs = ["turnaround_policy.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/qos",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/generated/params:QoSTurnaroundPolicy",
        "//src/mem/qos:workaround_that_cuts_circular_dependency__mem_ctrl",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
