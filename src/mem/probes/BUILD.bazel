# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
        "//src/mem/probes:base",
        "//src/mem/probes:mem_footprint",
        "//src/mem/probes:stack_dist",
    ] + select({
        "//src/generated/flags:have_protobuf_is_true": ["//src/mem/probes:mem_trace"],
        "//conditions:default": [],
    }) + [
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    macro_sim_object(
        "BaseMemProbe.py",
        enums = [],
        sim_objects = ["BaseMemProbe"],
    ),
    macro_sim_object(
        "MemFootprintProbe.py",
        enums = [],
        sim_objects = ["MemFootprintProbe"],
    ),
    macro_sim_object(
        "StackDistProbe.py",
        enums = [],
        sim_objects = ["StackDistProbe"],
    ),
] + select({
    "//src/generated/flags:have_protobuf_is_true": [
        macro_sim_object(
            "MemTraceProbe.py",
            enums = [],
            sim_objects = ["MemTraceProbe"],
        ),
    ],
    "//conditions:default": [],
}) + [
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "base",
    srcs = ["base.cc"],
    hdrs = ["base.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/probes",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/generated/params:BaseMemProbe",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
        "//src/sim/probe:mem",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "mem_footprint",
    srcs = ["mem_footprint.cc"],
    hdrs = ["mem_footprint.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/probes",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:callback",
        "//src/base:intmath",
        "//src/generated/params:MemFootprintProbe",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/mem/probes:base",
        "//src/sim:stats",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "stack_dist",
    srcs = ["stack_dist.cc"],
    hdrs = ["stack_dist.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/probes",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/generated/params:StackDistProbe",
        "//src/mem:stack_dist_calc",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/mem/probes:base",
        "//src/sim:stats",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "mem_trace",
    srcs = ["mem_trace.cc"],
    hdrs = ["mem_trace.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/probes",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:callback",
        "//src/base:output",
        "//src/generated/params:MemTraceProbe",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/mem/probes:base",
        "//src/proto:packet_pb",
        "//src/sim:core",
        "//src/sim:cur_tick",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ] + select({
        "//src/generated/flags:have_protobuf_is_true": ["//src/proto:protoio"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
