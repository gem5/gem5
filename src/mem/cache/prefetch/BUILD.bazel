# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
        "//src/mem/cache/prefetch:access_map_pattern_matching",
        "//src/mem/cache/prefetch:base",
        "//src/mem/cache/prefetch:bop",
        "//src/mem/cache/prefetch:delta_correlating_prediction_tables",
        "//src/mem/cache/prefetch:indirect_memory",
        "//src/mem/cache/prefetch:irregular_stream_buffer",
        "//src/mem/cache/prefetch:multi",
        "//src/mem/cache/prefetch:pif",
        "//src/mem/cache/prefetch:queued",
        "//src/mem/cache/prefetch:sbooe",
        "//src/mem/cache/prefetch:signature_path",
        "//src/mem/cache/prefetch:signature_path_v2",
        "//src/mem/cache/prefetch:slim_ampm",
        "//src/mem/cache/prefetch:sms",
        "//src/mem/cache/prefetch:spatio_temporal_memory_streaming",
        "//src/mem/cache/prefetch:stride",
        "//src/mem/cache/prefetch:tagged",
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    macro_sim_object(
        "Prefetcher.py",
        enums = [],
        sim_objects = [
            "BasePrefetcher",
            "MultiPrefetcher",
            "QueuedPrefetcher",
            "StridePrefetcherHashedSetAssociative",
            "StridePrefetcher",
            "SmsPrefetcher",
            "TaggedPrefetcher",
            "IndirectMemoryPrefetcher",
            "SignaturePathPrefetcher",
            "SignaturePathPrefetcherV2",
            "AccessMapPatternMatching",
            "AMPMPrefetcher",
            "DeltaCorrelatingPredictionTables",
            "DCPTPrefetcher",
            "IrregularStreamBufferPrefetcher",
            "SlimAMPMPrefetcher",
            "BOPPrefetcher",
            "SBOOEPrefetcher",
            "STeMSPrefetcher",
            "PIFPrefetcher",
        ],
    ),
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "access_map_pattern_matching",
    srcs = ["access_map_pattern_matching.cc"],
    hdrs = ["access_map_pattern_matching.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/prefetch",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base/cache:associative_cache",
        "//src/generated/debug:HWPrefetch",
        "//src/generated/params:AMPMPrefetcher",
        "//src/generated/params:AccessMapPatternMatching",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/mem/cache/prefetch:queued",
        "//src/mem/cache/tags:tagged_entry",
        "//src/sim:workaround_that_cuts_circular_dependency__clocked_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "base",
    srcs = ["base.cc"],
    hdrs = ["base.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/prefetch",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/generic:tlb",
        "//src/base:compiler",
        "//src/base:intmath",
        "//src/base:types",
        "//src/base:workaround_that_cuts_circular_dependency__statistics",
        "//src/generated/params:BasePrefetcher",
        "//src/mem:request",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/mem/cache:cache_probe_arg",
        "//src/mem/cache:workaround_that_cuts_circular_dependency__base",
        "//src/sim:byteswap",
        "//src/sim:workaround_that_cuts_circular_dependency__clocked_object",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
        "//src/sim/probe:workaround_that_cuts_circular_dependency__probe",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "bop",
    srcs = ["bop.cc"],
    hdrs = ["bop.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/prefetch",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/generated/debug:HWPrefetch",
        "//src/generated/params:BOPPrefetcher",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/mem/cache/prefetch:queued",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "delta_correlating_prediction_tables",
    srcs = ["delta_correlating_prediction_tables.cc"],
    hdrs = ["delta_correlating_prediction_tables.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/prefetch",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:circular_queue",
        "//src/base/cache:associative_cache",
        "//src/generated/debug:HWPrefetch",
        "//src/generated/params:DCPTPrefetcher",
        "//src/generated/params:DeltaCorrelatingPredictionTables",
        "//src/mem/cache/prefetch:queued",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "indirect_memory",
    srcs = ["indirect_memory.cc"],
    hdrs = ["indirect_memory.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/prefetch",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:sat_counter",
        "//src/base/cache:associative_cache",
        "//src/generated/params:IndirectMemoryPrefetcher",
        "//src/mem/cache:workaround_that_cuts_circular_dependency__base",
        "//src/mem/cache/prefetch:queued",
        "//src/mem/cache/tags:tagged_entry",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "irregular_stream_buffer",
    srcs = ["irregular_stream_buffer.cc"],
    hdrs = ["irregular_stream_buffer.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/prefetch",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:callback",
        "//src/base:sat_counter",
        "//src/base/cache:associative_cache",
        "//src/generated/debug:HWPrefetch",
        "//src/generated/params:IrregularStreamBufferPrefetcher",
        "//src/mem/cache/prefetch:queued",
        "//src/mem/cache/tags:tagged_entry",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "multi",
    srcs = ["multi.cc"],
    hdrs = ["multi.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/prefetch",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/generated/params:MultiPrefetcher",
        "//src/mem/cache/prefetch:base",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "pif",
    srcs = ["pif.cc"],
    hdrs = ["pif.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/prefetch",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:circular_queue",
        "//src/base/cache:associative_cache",
        "//src/generated/debug:HWPrefetch",
        "//src/generated/params:PIFPrefetcher",
        "//src/mem/cache/prefetch:queued",
        "//src/mem/cache/tags:tagged_entry",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "queued",
    srcs = ["queued.cc"],
    hdrs = ["queued.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/prefetch",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/generic:tlb",
        "//src/arch/generic:workaround_that_cuts_circular_dependency__mmu",
        "//src/base:trace",
        "//src/base:types",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/base:workaround_that_cuts_circular_dependency__statistics",
        "//src/generated/debug:HWPrefetch",
        "//src/generated/debug:HWPrefetchQueue",
        "//src/generated/params:QueuedPrefetcher",
        "//src/mem:request",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/mem/cache:workaround_that_cuts_circular_dependency__base",
        "//src/mem/cache/prefetch:base",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "sbooe",
    srcs = ["sbooe.cc"],
    hdrs = ["sbooe.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/prefetch",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:circular_queue",
        "//src/generated/debug:HWPrefetch",
        "//src/generated/params:SBOOEPrefetcher",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/mem/cache/prefetch:queued",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "signature_path",
    srcs = ["signature_path.cc"],
    hdrs = ["signature_path.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/prefetch",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:sat_counter",
        "//src/base/cache:associative_cache",
        "//src/generated/debug:HWPrefetch",
        "//src/generated/params:SignaturePathPrefetcher",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/mem/cache/prefetch:queued",
        "//src/mem/cache/tags:tagged_entry",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "signature_path_v2",
    srcs = ["signature_path_v2.cc"],
    hdrs = ["signature_path_v2.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/prefetch",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base/cache:associative_cache",
        "//src/generated/debug:HWPrefetch",
        "//src/generated/params:SignaturePathPrefetcherV2",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/mem/cache/prefetch:signature_path",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "slim_ampm",
    srcs = ["slim_ampm.cc"],
    hdrs = ["slim_ampm.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/prefetch",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/generated/params:SlimAMPMPrefetcher",
        "//src/mem/cache/prefetch:access_map_pattern_matching",
        "//src/mem/cache/prefetch:delta_correlating_prediction_tables",
        "//src/mem/cache/prefetch:queued",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "sms",
    srcs = ["sms.cc"],
    hdrs = ["sms.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/prefetch",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/generated/debug:HWPrefetch",
        "//src/generated/params:SmsPrefetcher",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/mem/cache/prefetch:queued",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "spatio_temporal_memory_streaming",
    srcs = ["spatio_temporal_memory_streaming.cc"],
    hdrs = ["spatio_temporal_memory_streaming.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/prefetch",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:circular_queue",
        "//src/base:sat_counter",
        "//src/base/cache:associative_cache",
        "//src/generated/debug:HWPrefetch",
        "//src/generated/params:STeMSPrefetcher",
        "//src/mem/cache/prefetch:queued",
        "//src/mem/cache/tags:tagged_entry",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "stride",
    srcs = ["stride.cc"],
    hdrs = ["stride.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/prefetch",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:intmath",
        "//src/base:random",
        "//src/base:sat_counter",
        "//src/base:trace",
        "//src/base:types",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/base/cache:associative_cache",
        "//src/generated/debug:HWPrefetch",
        "//src/generated/params:StridePrefetcher",
        "//src/generated/params:StridePrefetcherHashedSetAssociative",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/mem/cache/prefetch:queued",
        "//src/mem/cache/replacement_policies:base",
        "//src/mem/cache/replacement_policies:replaceable_entry",
        "//src/mem/cache/tags:tagged_entry",
        "//src/mem/cache/tags/indexing_policies:set_associative",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "tagged",
    srcs = ["tagged.cc"],
    hdrs = ["tagged.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/prefetch",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/generated/params:TaggedPrefetcher",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/mem/cache/prefetch:queued",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
