# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
        "//src/mem/cache/compressors:base",
        "//src/mem/cache/compressors:base_delta",
        "//src/mem/cache/compressors:base_dictionary_compressor",
        "//src/mem/cache/compressors:cpack",
        "//src/mem/cache/compressors:fpc",
        "//src/mem/cache/compressors:fpcd",
        "//src/mem/cache/compressors:frequent_values",
        "//src/mem/cache/compressors:multi",
        "//src/mem/cache/compressors:perfect",
        "//src/mem/cache/compressors:repeated_qwords",
        "//src/mem/cache/compressors:zero",
        "//src/mem/cache/compressors/encoders:lib",
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    macro_sim_object(
        "Compressors.py",
        enums = [],
        sim_objects = [
            "BaseCacheCompressor",
            "BaseDictionaryCompressor",
            "Base64Delta8",
            "Base64Delta16",
            "Base64Delta32",
            "Base32Delta8",
            "Base32Delta16",
            "Base16Delta8",
            "CPack",
            "FPC",
            "FPCD",
            "FrequentValuesCompressor",
            "MultiCompressor",
            "PerfectCompressor",
            "RepeatedQwordsCompressor",
            "ZeroCompressor",
        ],
    ),
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        "//src/mem/cache/compressors/encoders:sim_objects",
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "base",
    srcs = ["base.cc"],
    hdrs = ["base.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/compressors",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:trace",
        "//src/base:types",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/base:workaround_that_cuts_circular_dependency__statistics",
        "//src/generated/debug:CacheComp",
        "//src/generated/params:BaseCacheCompressor",
        "//src/mem/cache:workaround_that_cuts_circular_dependency__base",
        "//src/mem/cache/tags:super_blk",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "base_delta",
    srcs = ["base_delta.cc"],
    hdrs = ["base_delta.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/compressors",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:bitfield",
        "//src/base:trace",
        "//src/generated/params:Base16Delta8",
        "//src/generated/params:Base32Delta16",
        "//src/generated/params:Base32Delta8",
        "//src/generated/params:Base64Delta16",
        "//src/generated/params:Base64Delta32",
        "//src/generated/params:Base64Delta8",
        "//src/mem/cache/compressors:base_delta_impl",
        "//src/mem/cache/compressors:dictionary_compressor",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "base_dictionary_compressor",
    srcs = ["base_dictionary_compressor.cc"],
    hdrs = [],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/compressors",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/generated/params:BaseDictionaryCompressor",
        "//src/mem/cache/compressors:dictionary_compressor",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "cpack",
    srcs = ["cpack.cc"],
    hdrs = ["cpack.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/compressors",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:types",
        "//src/generated/params:CPack",
        "//src/mem/cache/compressors:dictionary_compressor",
        "//src/mem/cache/compressors:dictionary_compressor_impl",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "fpc",
    srcs = ["fpc.cc"],
    hdrs = ["fpc.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/compressors",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:bitfield",
        "//src/base:types",
        "//src/generated/params:FPC",
        "//src/mem/cache/compressors:dictionary_compressor",
        "//src/mem/cache/compressors:dictionary_compressor_impl",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "fpcd",
    srcs = ["fpcd.cc"],
    hdrs = ["fpcd.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/compressors",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:types",
        "//src/generated/params:FPCD",
        "//src/mem/cache/compressors:dictionary_compressor",
        "//src/mem/cache/compressors:dictionary_compressor_impl",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "frequent_values",
    srcs = ["frequent_values.cc"],
    hdrs = ["frequent_values.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/compressors",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:bitfield",
        "//src/base:compiler",
        "//src/base:intmath",
        "//src/base:sat_counter",
        "//src/base:types",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/base/cache:associative_cache",
        "//src/generated/debug:CacheComp",
        "//src/generated/params:FrequentValuesCompressor",
        "//src/mem/cache:cache_probe_arg",
        "//src/mem/cache:workaround_that_cuts_circular_dependency__base",
        "//src/mem/cache/compressors:workaround_that_cuts_circular_dependency__base",
        "//src/mem/cache/compressors/encoders:huffman",
        "//src/sim:eventq",
        "//src/sim/probe:workaround_that_cuts_circular_dependency__probe",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "multi",
    srcs = ["multi.cc"],
    hdrs = ["multi.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/compressors",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:bitfield",
        "//src/base:trace",
        "//src/base:types",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/base:workaround_that_cuts_circular_dependency__statistics",
        "//src/generated/debug:CacheComp",
        "//src/generated/params:MultiCompressor",
        "//src/mem/cache/compressors:workaround_that_cuts_circular_dependency__base",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "perfect",
    srcs = ["perfect.cc"],
    hdrs = ["perfect.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/compressors",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:types",
        "//src/generated/debug:CacheComp",
        "//src/generated/params:PerfectCompressor",
        "//src/mem/cache/compressors:workaround_that_cuts_circular_dependency__base",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "repeated_qwords",
    srcs = ["repeated_qwords.cc"],
    hdrs = ["repeated_qwords.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/compressors",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:trace",
        "//src/generated/debug:CacheComp",
        "//src/generated/params:RepeatedQwordsCompressor",
        "//src/mem/cache/compressors:dictionary_compressor",
        "//src/mem/cache/compressors:dictionary_compressor_impl",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "zero",
    srcs = ["zero.cc"],
    hdrs = ["zero.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/compressors",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:trace",
        "//src/generated/debug:CacheComp",
        "//src/generated/params:ZeroCompressor",
        "//src/mem/cache/compressors:dictionary_compressor",
        "//src/mem/cache/compressors:dictionary_compressor_impl",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "workaround_that_cuts_circular_dependency__base",
    srcs = [],
    hdrs = ["base.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/compressors",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:types",
        "//src/base:workaround_that_cuts_circular_dependency__statistics",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "workaround_that_cuts_circular_dependency__base_delta",
    srcs = [],
    hdrs = ["base_delta.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/compressors",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:bitfield",
        "//src/mem/cache/compressors:dictionary_compressor",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "base_delta_impl",
    srcs = [],
    hdrs = ["base_delta_impl.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/compressors",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/generated/debug:CacheComp",
        "//src/mem/cache/compressors:dictionary_compressor_impl",
        "//src/mem/cache/compressors:workaround_that_cuts_circular_dependency__base_delta",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "dictionary_compressor",
    srcs = [],
    hdrs = ["dictionary_compressor.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/compressors",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:bitfield",
        "//src/base:types",
        "//src/base:workaround_that_cuts_circular_dependency__statistics",
        "//src/mem/cache/compressors:workaround_that_cuts_circular_dependency__base",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "dictionary_compressor_impl",
    srcs = [],
    hdrs = ["dictionary_compressor_impl.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/compressors",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:trace",
        "//src/generated/debug:CacheComp",
        "//src/generated/params:BaseDictionaryCompressor",
        "//src/mem/cache/compressors:dictionary_compressor",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
