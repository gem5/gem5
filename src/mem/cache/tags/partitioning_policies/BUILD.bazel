# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
        "//src/mem/cache/tags/partitioning_policies:base_pp",
        "//src/mem/cache/tags/partitioning_policies:max_capacity_pp",
        "//src/mem/cache/tags/partitioning_policies:partition_manager",
        "//src/mem/cache/tags/partitioning_policies:way_allocation",
        "//src/mem/cache/tags/partitioning_policies:way_pp",
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
    macro_sim_object(
        "PartitioningPolicies.py",
        enums = [],
        sim_objects = [
            "PartitionManager",
            "BasePartitioningPolicy",
            "MaxCapacityPartitioningPolicy",
            "WayPolicyAllocation",
            "WayPartitioningPolicy",
        ],
    ),
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "base_pp",
    srcs = ["base_pp.cc"],
    hdrs = ["base_pp.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/tags/partitioning_policies",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/generated/params:BasePartitioningPolicy",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "max_capacity_pp",
    srcs = ["max_capacity_pp.cc"],
    hdrs = ["max_capacity_pp.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/tags/partitioning_policies",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/generated/debug:PartitionPolicy",
        "//src/generated/params:BasePartitioningPolicy",
        "//src/generated/params:MaxCapacityPartitioningPolicy",
        "//src/mem/cache:cache_blk",
        "//src/mem/cache/tags/partitioning_policies:base_pp",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "partition_manager",
    srcs = ["partition_manager.cc"],
    hdrs = ["partition_manager.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/tags/partitioning_policies",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/generated/params:PartitionManager",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/mem/cache/tags/partitioning_policies:base_pp",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "way_allocation",
    srcs = ["way_allocation.cc"],
    hdrs = ["way_allocation.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/tags/partitioning_policies",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/generated/params:WayPolicyAllocation",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "way_pp",
    srcs = ["way_pp.cc"],
    hdrs = ["way_pp.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "mem/cache/tags/partitioning_policies",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/generated/debug:PartitionPolicy",
        "//src/generated/params:WayPartitioningPolicy",
        "//src/mem/cache/replacement_policies:replaceable_entry",
        "//src/mem/cache/tags/partitioning_policies:base_pp",
        "//src/mem/cache/tags/partitioning_policies:way_allocation",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
