# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/cpu/testers/gpu_ruby_test:address_manager"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/cpu/testers/gpu_ruby_test:cpu_thread"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/cpu/testers/gpu_ruby_test:dma_thread"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/cpu/testers/gpu_ruby_test:episode"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/cpu/testers/gpu_ruby_test:gpu_wavefront"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/cpu/testers/gpu_ruby_test:protocol_tester"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/cpu/testers/gpu_ruby_test:tester_thread"],
        "//conditions:default": [],
    }) + [
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
] + select({
    "//src/generated/flags:build_gpu_is_true": [
        macro_sim_object(
            "CpuThread.py",
            enums = [],
            sim_objects = ["CpuThread"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:build_gpu_is_true": [
        macro_sim_object(
            "DmaThread.py",
            enums = [],
            sim_objects = ["DmaThread"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:build_gpu_is_true": [
        macro_sim_object(
            "GpuWavefront.py",
            enums = [],
            sim_objects = ["GpuWavefront"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:build_gpu_is_true": [
        macro_sim_object(
            "ProtocolTester.py",
            enums = [],
            sim_objects = ["ProtocolTester"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:build_gpu_is_true": [
        macro_sim_object(
            "TesterDma.py",
            enums = [],
            sim_objects = ["TesterDma"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:build_gpu_is_true": [
        macro_sim_object(
            "TesterThread.py",
            enums = [],
            sim_objects = ["TesterThread"],
        ),
    ],
    "//conditions:default": [],
}) + [
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "address_manager",
    srcs = ["address_manager.cc"],
    hdrs = ["address_manager.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "cpu/testers/gpu_ruby_test",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:intmath",
        "//src/base:random",
        "//src/base:trace",
        "//src/base:types",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/sim:eventq",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "cpu_thread",
    srcs = ["cpu_thread.cc"],
    hdrs = ["cpu_thread.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "cpu/testers/gpu_ruby_test",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/generated/debug:ProtocolTest",
        "//src/generated/params:CpuThread",
        "//src/sim:workaround_that_cuts_circular_dependency__clocked_object",
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/cpu/testers/gpu_ruby_test:tester_thread"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "dma_thread",
    srcs = ["dma_thread.cc"],
    hdrs = ["dma_thread.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "cpu/testers/gpu_ruby_test",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/generated/debug:ProtocolTest",
        "//src/generated/params:DmaThread",
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/cpu/testers/gpu_ruby_test:tester_thread"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "episode",
    srcs = ["episode.cc"],
    hdrs = ["episode.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "cpu/testers/gpu_ruby_test",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:random",
        "//src/generated/debug:EpisodeCount",
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/cpu/testers/gpu_ruby_test:address_manager"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/cpu/testers/gpu_ruby_test:protocol_tester"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/cpu/testers/gpu_ruby_test:tester_thread"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "gpu_wavefront",
    srcs = ["gpu_wavefront.cc"],
    hdrs = ["gpu_wavefront.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "cpu/testers/gpu_ruby_test",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/generated/debug:ProtocolTest",
        "//src/generated/params:GpuWavefront",
        "//src/sim:workaround_that_cuts_circular_dependency__clocked_object",
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/cpu/testers/gpu_ruby_test:tester_thread"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "protocol_tester",
    srcs = ["protocol_tester.cc"],
    hdrs = ["protocol_tester.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "cpu/testers/gpu_ruby_test",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:random",
        "//src/base:types",
        "//src/generated/debug:ProtocolTest",
        "//src/generated/params:ProtocolTester",
        "//src/mem:request",
        "//src/mem:token_port",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/mem/ruby/system:RubyPort",
        "//src/sim:sim_exit",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/cpu/testers/gpu_ruby_test:address_manager"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/cpu/testers/gpu_ruby_test:cpu_thread"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/cpu/testers/gpu_ruby_test:dma_thread"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/cpu/testers/gpu_ruby_test:gpu_wavefront"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/cpu/testers/gpu_ruby_test:tester_thread"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "tester_thread",
    srcs = ["tester_thread.cc"],
    hdrs = ["tester_thread.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "cpu/testers/gpu_ruby_test",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:random",
        "//src/generated/debug:ProtocolTest",
        "//src/mem:token_port",
        "//src/sim:workaround_that_cuts_circular_dependency__clocked_object",
    ] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/cpu/testers/gpu_ruby_test:address_manager"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/cpu/testers/gpu_ruby_test:episode"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/cpu/testers/gpu_ruby_test:protocol_tester"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:build_gpu_is_true": ["//src/gpu-compute:gpu_dyn_inst"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
