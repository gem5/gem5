# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
    ] + select({
        "//src/generated/flags:ruby_is_true": ["//src/cpu/testers/rubytest:Check"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:ruby_is_true": ["//src/cpu/testers/rubytest:CheckTable"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:ruby_is_true": ["//src/cpu/testers/rubytest:RubyTester"],
        "//conditions:default": [],
    }) + [
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
] + select({
    "//src/generated/flags:ruby_is_true": [
        macro_sim_object(
            "RubyTester.py",
            enums = [],
            sim_objects = ["RubyTester"],
        ),
    ],
    "//conditions:default": [],
}) + [
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "Check",
    srcs = ["Check.cc"],
    hdrs = ["Check.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "cpu/testers/rubytest",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:random",
        "//src/base:trace",
        "//src/generated/debug:RubyTest",
        "//src/mem/ruby/common:Address",
        "//src/mem/ruby/common:SubBlock",
        "//src/mem/ruby/protocol:RubyAccessMode",
        "//src/mem/ruby/protocol:TesterStatus",
    ] + select({
        "//src/generated/flags:ruby_is_true": ["//src/cpu/testers/rubytest:RubyTester"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "CheckTable",
    srcs = ["CheckTable.cc"],
    hdrs = ["CheckTable.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "cpu/testers/rubytest",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:intmath",
        "//src/base:random",
        "//src/base:trace",
        "//src/generated/debug:RubyTest",
        "//src/mem/ruby/common:Address",
    ] + select({
        "//src/generated/flags:ruby_is_true": ["//src/cpu/testers/rubytest:Check"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "RubyTester",
    srcs = ["RubyTester.cc"],
    hdrs = ["RubyTester.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "cpu/testers/rubytest",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/generated/debug:RubyTest",
        "//src/generated/params:RubyTester",
        "//src/mem:port",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/mem/ruby/common:SubBlock",
        "//src/mem/ruby/common:TypeDefines",
        "//src/sim:sim_exit",
        "//src/sim:workaround_that_cuts_circular_dependency__clocked_object",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ] + select({
        "//src/generated/flags:ruby_is_true": ["//src/cpu/testers/rubytest:Check"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:ruby_is_true": ["//src/cpu/testers/rubytest:CheckTable"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
