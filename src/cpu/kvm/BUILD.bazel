# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
    ] + select({
        "//src/generated/flags:use_kvm_is_true": ["//src/cpu/kvm:base"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_kvm_is_true": ["//src/cpu/kvm:device"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_kvm_is_true": ["//src/cpu/kvm:perfevent"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_kvm_is_true": ["//src/cpu/kvm:timer"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:use_kvm_is_true": ["//src/cpu/kvm:vm"],
        "//conditions:default": [],
    }) + [
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
] + select({
    "//src/generated/flags:use_kvm_is_true": [
        macro_sim_object(
            "BaseKvmCPU.py",
            enums = [],
            sim_objects = ["BaseKvmCPU"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:use_kvm_is_true": [
        macro_sim_object(
            "KvmVM.py",
            enums = [],
            sim_objects = ["KvmVM"],
        ),
    ],
    "//conditions:default": [],
}) + [
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "base",
    srcs = ["base.cc"],
    hdrs = ["base.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "cpu/kvm",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:compiler",
        "//src/base:workaround_that_cuts_circular_dependency__statistics",
        "//src/cpu:workaround_that_cuts_circular_dependency__base",
        "//src/cpu:workaround_that_cuts_circular_dependency__simple_thread",
        "//src/cpu/kvm:workaround_that_cuts_circular_dependency__vm",
        "//src/generated/debug:Checkpoint",
        "//src/generated/debug:Drain",
        "//src/generated/debug:Kvm",
        "//src/generated/debug:KvmIO",
        "//src/generated/debug:KvmRun",
        "//src/generated/params:BaseKvmCPU",
        "//src/sim:faults",
        "//src/sim:workaround_that_cuts_circular_dependency__process",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ] + select({
        "//src/generated/flags:use_kvm_is_true": ["//src/cpu/kvm:perfevent"],
        "//conditions:default": [],
    }) + [] + select({
        "//src/generated/flags:use_kvm_is_true": ["//src/cpu/kvm:timer"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "device",
    srcs = ["device.cc"],
    hdrs = ["device.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "cpu/kvm",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/base:workaround_that_cuts_circular_dependency__logging"],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "perfevent",
    srcs = ["perfevent.cc"],
    hdrs = ["perfevent.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "cpu/kvm",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/generated/config:have_perf_attr_exclude_host",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "timer",
    srcs = ["timer.cc"],
    hdrs = ["timer.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "cpu/kvm",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__logging",
        "//src/generated/debug:KvmTimer",
        "//src/sim:core",
    ] + select({
        "//src/generated/flags:use_kvm_is_true": ["//src/cpu/kvm:perfevent"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "vm",
    srcs = ["vm.cc"],
    hdrs = ["vm.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "cpu/kvm",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:addr_range",
        "//src/generated/debug:Kvm",
        "//src/generated/params:KvmVM",
        "//src/mem:physical",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
        "//src/sim:workaround_that_cuts_circular_dependency__system",
    ] + select({
        "//src/generated/flags:use_kvm_is_true": ["//src/cpu/kvm:base"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "workaround_that_cuts_circular_dependency__vm",
    srcs = [],
    hdrs = ["vm.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "cpu/kvm",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:addr_range",
        "//src/sim:workaround_that_cuts_circular_dependency__sim_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
