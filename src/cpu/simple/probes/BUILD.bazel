# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
    ] + select({
        "//src/generated/flags:build_isa_is_true": ["//src/cpu/simple/probes:looppoint_analysis"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:build_isa_is_true": ["//src/cpu/simple/probes:simpoint"],
        "//conditions:default": [],
    }) + [
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
] + select({
    "//src/generated/flags:build_isa_is_true": [
        macro_sim_object(
            "LooppointAnalysis.py",
            enums = [],
            sim_objects = [
                "LooppointAnalysis",
                "LooppointAnalysisManager",
            ],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:build_isa_is_true": [
        macro_sim_object(
            "SimPoint.py",
            enums = [],
            sim_objects = ["SimPoint"],
        ),
    ],
    "//conditions:default": [],
}) + [
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "looppoint_analysis",
    srcs = ["looppoint_analysis.cc"],
    hdrs = ["looppoint_analysis.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "cpu/simple/probes",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/arch/generic:pcstate",
        "//src/cpu:workaround_that_cuts_circular_dependency__simple_thread",
        "//src/generated/debug:LooppointAnalysis",
        "//src/generated/params:LooppointAnalysis",
        "//src/generated/params:LooppointAnalysisManager",
        "//src/sim:sim_exit",
        "//src/sim/probe:probe_listener_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "simpoint",
    srcs = ["simpoint.cc"],
    hdrs = ["simpoint.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "cpu/simple/probes",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:output",
        "//src/cpu:workaround_that_cuts_circular_dependency__simple_thread",
        "//src/generated/params:SimPoint",
        "//src/sim/probe:probe_listener_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
