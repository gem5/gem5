# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
    ] + select({
        "//src/generated/flags:have_protobuf_is_true": ["//src/cpu/o3/probe:elastic_trace"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:build_isa_is_true": ["//src/cpu/o3/probe:simple_trace"],
        "//conditions:default": [],
    }) + [
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
] + select({
    "//src/generated/flags:have_protobuf_is_true": [
        macro_sim_object(
            "ElasticTrace.py",
            enums = [],
            sim_objects = ["ElasticTrace"],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:build_isa_is_true": [
        macro_sim_object(
            "SimpleTrace.py",
            enums = [],
            sim_objects = ["SimpleTrace"],
        ),
    ],
    "//conditions:default": [],
}) + [
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "elastic_trace",
    srcs = ["elastic_trace.cc"],
    hdrs = ["elastic_trace.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "cpu/o3/probe",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:callback",
        "//src/base:output",
        "//src/base:trace",
        "//src/base:workaround_that_cuts_circular_dependency__statistics",
        "//src/cpu:reg_class",
        "//src/cpu/o3:dyn_inst_ptr",
        "//src/cpu/o3:workaround_that_cuts_circular_dependency__dyn_inst",
        "//src/generated/debug:ElasticTrace",
        "//src/generated/params:ElasticTrace",
        "//src/mem:request",
        "//src/mem:workaround_that_cuts_circular_dependency__packet",
        "//src/proto:inst_dep_record_pb",
        "//src/proto:packet_pb",
        "//src/sim:eventq",
        "//src/sim/probe:probe_listener_object",
    ] + select({
        "//src/generated/flags:have_protobuf_is_true": ["//src/proto:protoio"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "simple_trace",
    srcs = ["simple_trace.cc"],
    hdrs = ["simple_trace.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "cpu/o3/probe",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/base:trace",
        "//src/cpu/o3:dyn_inst_ptr",
        "//src/cpu/o3:workaround_that_cuts_circular_dependency__dyn_inst",
        "//src/generated/debug:SimpleTrace",
        "//src/generated/params:SimpleTrace",
        "//src/sim/probe:probe_listener_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
