# GENERATED BY @bazelize_gem5//scons2bzl:sync
# See "build_tools/bazel/README.md" when in doubt
load("@bazel_skylib//rules:common_settings.bzl", "bool_flag", "int_flag", "string_flag")
load("//build_tools/bazel:macro.bzl", "macro_py_source", "macro_sim_object")
load(
    "//build_tools/bazel:rules.bzl",
    "COMMON_COPTS",
    "COMMON_LINKOPTS",
    "debug_flag",
    "gem5_py_source",
    "gen_config",
)

cc_library(
    name = "lib",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        # OBJS_GOES_HERE.START
    ] + select({
        "//src/generated/flags:build_isa_is_true": ["//src/cpu/probes:inst_tracker"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:build_isa_is_true": ["//src/cpu/probes:pc_count_tracker"],
        "//conditions:default": [],
    }) + [
    ] + select({
        "//src/generated/flags:build_isa_is_true": ["//src/cpu/probes:pc_count_tracker_manager"],
        "//conditions:default": [],
    }) + [
        # OBJS_GOES_HERE.END
    ],
)

sim_objects = [
    # SIMOBJ_GOES_HERE.START
] + select({
    "//src/generated/flags:build_isa_is_true": [
        macro_sim_object(
            "InstTracker.py",
            enums = [],
            sim_objects = [
                "GlobalInstTracker",
                "LocalInstTracker",
            ],
        ),
    ],
    "//conditions:default": [],
}) + [
] + select({
    "//src/generated/flags:build_isa_is_true": [
        macro_sim_object(
            "PcCountTracker.py",
            enums = [],
            sim_objects = [
                "PcCountTracker",
                "PcCountTrackerManager",
            ],
        ),
    ],
    "//conditions:default": [],
}) + [
    # SIMOBJ_GOES_HERE.END
]

cc_library(
    name = "sim_objects",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = sim_objects + [
        # SIMOBJ_LIB_GOES_HERE.START
        # SIMOBJ_LIB_GOES_HERE.END
    ],
)

# DEBUG_FLAG_GOES_HERE.START
# DEBUG_FLAG_GOES_HERE.END

# TARGET_GOES_HERE.START

cc_library(
    name = "inst_tracker",
    srcs = ["inst_tracker.cc"],
    hdrs = ["inst_tracker.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "cpu/probes",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/generated/debug:InstTracker",
        "//src/generated/params:GlobalInstTracker",
        "//src/generated/params:LocalInstTracker",
        "//src/sim:sim_exit",
        "//src/sim/probe:probe_listener_object",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "pc_count_tracker",
    srcs = ["pc_count_tracker.cc"],
    hdrs = ["pc_count_tracker.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "cpu/probes",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/generated/params:PcCountTracker",
        "//src/sim/probe:probe_listener_object",
    ] + select({
        "//src/generated/flags:build_isa_is_true": ["//src/cpu/probes:pc_count_tracker_manager"],
        "//conditions:default": [],
    }) + [],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "pc_count_tracker_manager",
    srcs = ["pc_count_tracker_manager.cc"],
    hdrs = ["pc_count_tracker_manager.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "cpu/probes",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = [
        "//src/cpu:workaround_that_cuts_circular_dependency__base",
        "//src/generated/debug:PcCountTracker",
        "//src/generated/params:PcCountTrackerManager",
        "//src/sim:sim_exit",
    ],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)

cc_library(
    name = "pc_count_pair",
    srcs = [],
    hdrs = ["pc_count_pair.hh"],
    copts = COMMON_COPTS + [],
    include_prefix = "cpu/probes",
    visibility = ["//build_tools/bazel/package_group:simulator"],
    deps = ["//src/base:types"],
    alwayslink = True,  # Extra care needs to be taken on `deps` ordering to remove this
)
# TARGET_GOES_HERE.END
